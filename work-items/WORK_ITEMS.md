# Work Items

Active feature queue for Ivy. Game Director and Systems Designer pull from here.

## Current phase

**Phase 1 — Ivy simulator on a small brick tower.** Scope, goals, success criteria, non-goals, and
milestones M1–M4 (plus **M2.5 — Visible causation**) are defined in `DESIGN.md`. Technical spec is
`ivy_simulator_v0.1.md`.

Phase 1 answers two questions only: *does the simulator work*, and *does it look good*. No
gamification.

**Sequencing rule (director-level, not negotiable):** no parameter tuning, no field-precision work,
and no leaf-art polish before M1 is on screen. Get ugly ivy on the tower first.

**Stage (updated 2026-08-11):** M2 SHIPPED. **Director sequencing decision (SD-OPEN-8):** insert
**M2.5 — Visible causation** before M3. W-046, W-059, W-062, W-060, W-002, W-061 are all **done**.
**LG-2 was resolved by withdrawal, not by tuning.** Measured LG-2a Δg=0.00834 against a ≥0.03 bar;
the Systems Designer proved by inverting the linear tint rule that the gate's premise was wrong
(leaf-weighted shaded `f̄_L` ≈ 0.95, not the surface value 0.736 used at ratification) because
survivorship sampling means the darkest cells hold almost no leaf area to measure. Director ratified
**SD-OPEN-10 / W-074**: LG-2a and LG-2b withdrawn, AS-1 and AS-2 named the automatable causation
backstop, LG-1 the human gate, and **LG-2′** substituted. The **AS-1 knife-edge** was ruled a category
error and resolved the same way — `s_light` comes out of the coverage metric (**W-076**) rather than
the floor moving. No leaf parameter changed and tint widening stays rejected.

**LG-4 PASSED — verified 2026-08-11.** Both clauses. Day/night: mean grey 0.347 / 0.446 / 0.348 /
0.030 at 06:00 / noon / 18:00 / midnight, all four frames mutually distinct, midnight at 6.7% of
noon, and dawn distinguishable from dusk (RMSE 3,979) — so the sun moves rather than merely dimming,
which a brightness-only check would not have caught. Slightly darker than the W-059 baseline at every
frame, consistent with the sky and tint edits that followed it; the script guards the day/night
relationship, not absolute exposure, and the stale baseline in the run-ui-script skill has been
corrected. Brick surfacing: the noon bare-tower frame shows clear repeating brick coursing, not the
smooth gradient the clause was written to fail. Note the known W-068 horizon band is visible in the
same frame and remains deferred to M4.

**LG-1 PASSED — owner sign-off 2026-08-11.** Judged on the matched day-150 CamSun/CamShade mirror
pair (same height, same distance, same moment, only the side differs): the sun/shade difference is
legible without being told which is which. This is the gate M3's seed anchors were waiting on
(W-012 unblocked) and it is the human half of the causation evidence that replaced LG-2. Owner note
filed as W-080: the weathered leaves currently read as evenly scattered reddish speckle, closer to
autumn or disease than weathering — deferred to the M4 visual pass, not a gate.

**Remaining before M2.5 exit.** W-076 has **landed** (shade 50.09% on a fixed leaf-`a` reference
footprint), with two unfalsifiable guards being fixed. Three items now sit with the **Director** and
are entangled, so they want one ruling rather than three: **W-078** (the floor is met at 50.09% but
the margin is 0.09 pt, functionally zero, and the reference-leaf choice is worth about the whole
margin), **W-079** (the cant costs shade 1.77 pt — 20× that margin — and was deferred to M4 on a
prediction the diagnostic has now contradicted), and **W-077** (lock the measured LG-2′ decile
threshold). Then **W-082** builds the restated LG-3 comparison (W-073 is ratified as SD-OPEN-11 and
closes with it), **W-081** confirms the LG-3 tolerance without blocking the provisional pass, and
**LG-4** is re-verified now that a real sky exists. Then Code Reviewer over the whole M2.5 change set → Fixer → QA. Owner action on
W-024 before M4 rubric scoring only.

**Milestone reality.** M1's gate is met: recognizably ivy-shaped growth appears in
`.tmp/ui_scripts/m1_exit.png`, and the run is reproducible at **2295 segments / 1027 leaves / 66.6238 m**
of stem over 30 game-days (updated after W-036 landed leaf crowding deposits; two back-to-back
runs reproduced identically to the bit — INV-7 intact).

**M2 gate verdict (Director 2026-08-09): SHIPPED.** AS-2 and AS-3(a)/(b)/(c) pass under SD-OPEN-7
ratified instruments; determinism confirmed: day 150 = 43,743 segs / 18,339 leaves / 1285.07008928194 m /
128 dormant / AS-1 74.58 / 96.23 / 50.62 (⚠️ shaded +0.62 pts — M4 tension, not M2 gate). AS-1 by
volume does not block M2. Leaf speckling / rubric-2 placement efficiency is M4 (W-015, W-030, W-046).
Canonical cameras 2–4 still owed (W-002) — AS-6 judged from M1 camera only. Mechanical follow-up:
W-053 (test tolerance 1e-6 + INV-3a directional-term guard).

M3 and M4 are untouched. **M2.5 is the active milestone.**

**Implementation order (Director, SD-OPEN-8; supersedes `IMPLEMENTATION.md` § AR-SEQ Stage 3/4
ordering until Architect ratifies).** Work the queue in this order, not by ID:

| Stage | Items | Gate |
|---|---|---|
| **0 — Foundations** | W-026 (`IvyParams`) → W-017 (`Conv`) → W-018 (`Hash64` / `RngStream` / `SimClock`) | Unit tests green; nothing on screen yet |
| **1 — M1 slice** | W-025 (3D scene) → W-002 → W-003 → W-004 → W-005 → W-006 → W-007 | **Recognizably ivy-shaped growth in a screenshot** |
| **2 — M2** | W-008 → W-019 → W-009 → W-010 → W-011 → W-021 → W-020 → W-022 | AS-2 and AS-3 pass |
| **2.5 — M2.5** | W-046 → W-059 (sky) → W-060 (legibility leaves) → W-002 (four canonical cameras) → W-061 → W-076 (amended) → W-075 → W-077 | **LG-1 (passed), LG-2′, LG-3, LG-4** — LG-2 withdrawn per W-074 |
| **3 — M3** | W-012 → W-013 → W-027 | A parameter change is visible without a restart. **W-012 blocked on M2.5.** |
| **4 — M4** | W-015 → W-030 (remainder) → W-014 (scoring gated on W-024) | Blacklist clean, rubric ≥5/6, AS-1/4/5/6 pass |

Two ordering notes that are not obvious from the IDs. W-019 lands **before** W-009: building the
light field against live raycasts and retrofitting the bake afterwards means writing the EWMA loop
twice. And the whole of Stage 0 lands before any 3D content, because `SD-CONV` is the highest-risk
detail in the project and its conformance tests are worthless once geometry already depends on a
wrong convention.

## Queue

| ID | Title | Milestone | Stage | Status | Notes |
|----|-------|-----------|-------|--------|-------|
| W-001 | Define Phase 1 player loop in DESIGN.md | — | — | done | `DESIGN.md` rewritten 2026-08-08: vision, agency, goals G1–G6, invariants INV-1–INV-10, acceptance signals AS-1–AS-6, artifact blacklist, director rubric, non-goals, risks R-1–R-6, open decisions D-1–D-5. Handoff to Systems Designer. |
| W-016 | Phase 1 system contracts and D-1–D-5 resolutions | — | — | done | `IMPLEMENTATION.md` "Phase 1 System Contracts" 2026-08-08: SD-CONV coordinate audit, SD-TIME (D-1), SD-ENV, SD-PHYS, SD-GEO, SD-TIP (D-3), SD-LEAF, SD-STEM, SD-AGENCY (D-4), SD-METRIC (D-2, D-5), SD-RNG, SD-EDGE, SD-M1 split, SD-PARAM, SD-SCEN, SD-OPEN. Handoff to Gameplay Architect. |
| W-002 | Phase 1 tower scene, ground, sky, four canonical cameras | M2.5 | Gameplay Programmer | **done** | **Landed 2026-08-11.** All four canonical cameras implemented in `main.tscn` under `CameraRig`: CamSun (0), CamShade (1), CamTop (2, 45° pitch-down), CamSilhouette (3, 10.6° pitch-up ground). `CameraRig` (`src/world/camera_rig.gd`) manages `current` toggles only — AR-SCENE-3. 4 unit tests in `test/test_camera_rig.gd`. **Bug W-069 found and fixed:** CamTop and CamSilhouette had row/column confusion in Transform3D constructor — CamTop pointed at sky, CamSilhouette at ground; both corrected. LG-3: all 4 PNGs byte-identical across two consecutive `qa_m25_canonical.txt` runs under caffeinate (W-070) — but see W-073, where a later pair differed on 0.029% of pixels by 1/255. **Follow-ups 2026-08-11:** CamShade reframed as the exact mirror of CamSun (W-072); aim independently verified by `tools/dump_cameras.gd`, which prints each parsed transform's origin, forward vector, pitch and the height where its view axis crosses the tower axis. That tool was added because reasoning about whether the `.tscn` 12-float `Transform3D` form is row- or column-major produced two contradictory answers — one agreeing with the rendered frames, one with a unit test. It is **row-major**: `basis.z` is the column `(xz, yz, zz)`, so a camera's view direction is `−(xz, yz, zz)`. Suite 176/176. |
| W-003 | Environment field skeleton with constant-light stub | M1 | Code Reviewer | done | **Landed:** `sparse_field.gd` and `cell_grid.gd` provide the three-layer shell with trilinear reads, hashed sample jitter and central differences; no nearest-cell accessor is exposed. A double-scaling bug in the jitter (`field_sample_jitter()` multiplied by `field_cell` twice) was found and fixed. The constant `D_L = 12` stub was replaced by the real field at W-009. Original spec: Sparse hash field, `field_cell = 0.06 m`. Trilinear reads only, hashed sample jitter, central differences at `ε = 1.5·cell` (SD-ENV-1/3/4/5 — this is the R-3 mitigation and must be in from the start). `f_M` and weather pinned 1.0 (INV-9). M1: constant `D_L = 12`. **Architecture (AR-FIELD):** allocate a three-layer shell (`\|Φ\| ≤ 0.09 m`, ~40k cells) because trilinear interpolation of the single-layer shell SD-ENV-1 describes is undefined (AR-AMBIG-1); project every sample onto the shell along `∇̂Φ` before lookup, or floating tips read a fabricated radial gradient that destroys the AS-6 sag (AR-AMBIG-2); store `material_id` per cell, not resolved `A_m` (AR-AMBIG-3). No nearest-cell accessor may exist on the public API. |
| W-004 | Plant physiology layer | M1 | Code Reviewer | done | **Landed:** `physiology.gd` implements the full factor stack and the per-tick segment bound, and is the only writer to the crowding channel. Original spec: `f_L`, `f_C`, `f_S`, `H`, all weights, `r`, budget `B` — full from M1 (SD-M1). `max_segments_per_tick = 8` bound (SD-PHYS-2). Physiology is the only writer to the crowding channel, including predicted leaf area (SD-PHYS-3, INV-1). |
| W-005 | Growth geometry core | M1 | Code Reviewer | done | **Landed:** `growth_step.gd` implements the §21 direction sum, both distance thresholds and the degenerate guards, and owns the ground-strike rule that was wrongly duplicated in `tip_manager`. **Open question against this item:** the direction sum has no upward-bias term, which is W-032. Original spec: Full §21 sum with **`G = (0,−1,0)`** (SD-CONV-1). Two thresholds: `d_A = 0.15 m` adhesion range vs `d_contact = 0.02 m` attachment (SD-GEO-1). Degenerate-direction and zero-distance guards (SD-GEO-2). No ivy–ivy collision (SD-GEO-3). |
| W-006 | Stem mesh generation | M1 | Code Reviewer | done | **Landed:** `stem_mesh.gd` does chunked incremental append with the hot chunk flushed to its `MeshInstance3D` on every sync (W-029 fixed the original, which left the last 0–127 growing segments invisible). M1's fixed-radius 5-sided tube only — taper by branch order and Catmull-Rom smoothing are deferred to W-015. Original spec: Incremental append only, never rebuild (SD-GEO-7). M1: fixed-radius 5-sided tube. Full: taper by branch order and tip distance + Catmull-Rom path smoothing, which is the actual R-4 mitigation (SD-STEM). **Architecture (AR-RENDER-1/2):** chunked append — a bounded hot chunk sealed into frozen `MeshInstance3D`s. Tip taper mutates already-frozen segments, so at M4 it moves to the vertex shader driven by a per-tip length texture; Catmull-Rom needs the next segment, so tube emission lags by one segment and the trailing 3 cm of each live tip is simply not drawn (AR-AMBIG-8). |
| W-007 | Leaf system — minimal | M1 | Code Reviewer | done | **Landed:** `leaf_placer.gd` in `src/sim/` drives placement, `leaf_renderer.gd` copies into a `MultiMesh` with the atlas `ShaderMaterial`, and `msaa_3d = 2` is set. A deadlock where the tip-suppression gate compared `shoot_length - distance_since_node` (always zero) meant no leaf was ever placed; the gate now tests `tip.shoot_length` directly. Golden-angle phyllotaxy and shade etiolation arrived early in the Fixer pass. The remaining SD-LEAF-4..8 richness is W-030. Original spec: M1 subset per SD-M1: flat quad, atlas leaf `a`, fixed 0.075 m, ±90° alternation, 0.04 m internode, **normal-offset ladder ON** (SD-LEAF-4 rule 8 — costs nothing, without it M1 z-fights and teaches nothing). Node placement is per metre grown, never per tick (SD-LEAF-2, INV-4). **Architecture (AR-OVER-2, AR-RENDER-3/4):** leaf transforms are *simulation* output — every attribute comes from a per-tip RNG draw — so `LeafPlacer` lives in `src/sim/` and the renderer only copies into a `MultiMesh`. Requires a `ShaderMaterial` (a `StandardMaterial3D` cannot pick an atlas sub-rect per instance) and the `msaa_3d = 2` project setting, without which SD-LEAF-1's alpha-to-coverage is a silent no-op (AR-AMBIG-11). |
| W-008 | Solar position and day/night cycle | M2 | Code Reviewer | done | **Landed:** `src/env/solar.gd` provides solar position, day length, sunrise/sunset and the diel gate; `world.gd` drives the `DirectionalLight3D`. The sun was initially inverted (`look_at` aimed the light at the sun rather than away from it), which is why the first M1 run rendered an unlit tower. Original spec: D-1 resolved (SD-TIME). Fixed 1-game-hour tick at all speeds; three speeds; render-sun blend at time-lapse rates; lat 51.5 N, lon 0, DOY 105, start 06:00. Sun frame per SD-CONV-2; light forward = −S per SD-CONV-9. |
| W-009 | Light field: instantaneous, visibility, sky-view, accumulated | M2 | Code Reviewer | done | **Landed:** `light_bake.gd` and a rewritten `environment.gd` — EWMA at `τ_L = 3`, closed-form steady-state warm-up, `invalidate()` re-bake, surface gradient. Measured against the SD-ENV-10 regression targets: south `D_L` ≈ 23.8 (target ≈26), mean north ≈ 4.3 (target ≈2.5). Two residual issues are tracked separately — the inflated north point-probe is W-031 and the saturating east-face gradient is W-035. Original spec: `P`, `V`, `SVF`, incidence, `D_L` with `τ_L = 3`, surface gradient. Bake the hourly `P(cell,hour)` table, `SVF`, and the 24-bit `V_hours` mask behind `environment.invalidate(aabb)` (SD-ENV-6/7). Regression target: south `D_L ≈ 26`, north `D_L ≈ 2.5` (SD-ENV-10). |
| W-010 | Crowding field and crowding gradient | M2 | Code Reviewer | done | **Landed (completed by W-036, 2026-08-09):** the crowding channel is written by `physiology.gd` (sole writer, INV-1) and read by `growth_step.gd` and `tip_manager.gd`. W-036 completed the two missing halves: `Physiology.deposit_leaf_crowding()` deposits `k_leaf·leaf_area/cell_area` per node (SD-PHYS-3), and `leaf_placer.gd` suppresses placement at `P = clamp(1−0.55·C, 0.35, 1)` (SD-LEAF-8). Both contracts are now in place. Original spec: `C(x)`, surface gradient, effect on growth and branching. Leaves deposit most of the crowding via predicted area computed in physiology (SD-LEAF-8, SD-PHYS-3). |
| W-011 | Branching, tip lifecycle, tip cap | M2 | Code Reviewer | done | **Landed:** `tip_manager.gd` implements the soft 96 / hard 160 cap with tapered `branch_probability_scale` between them and vigour-based retirement to DORMANT; regression coverage in `test_tip_lifecycle.gd`. The original counted ground strikes from tip position alone, which doomed a seed placed at `ground_y_min` by tick 5. Original spec: D-3 resolved (SD-TIP): soft 96 / hard 160, taper between, vigour-based retirement to DORMANT, floating and top-of-tower exemptions. Full state machine incl. stall rule. Geometry of dormant/dead tips persists (INV-2). |
| W-012 | Seed anchors and time controls | M3 | Gameplay Programmer | open | D-4 resolved (SD-AGENCY). **Blocked on M2.5 (SD-OPEN-8):** seed choice is meaningless until LG-1 passes — the player must be able to *see* sun/shade asymmetry before choosing an anchor. Time controls (pause, 3 speeds, date readout, time-lapse indicator) may land earlier if they do not expose anchors. Re-seed resets plant + crowding, re-warms light field. |
| W-013 | Dev tuning overlay and field visualization | M3 | Gameplay Programmer | open | Spec §30 **plus the SD-PARAM additions** live-tunable without restart (INV-6, G6). Edits affect future growth only (SD-EDGE-10). Field visualization for `D_L`/`C`, and the AS-3 readouts incl. the diel gate `ĝ`. |
| W-014 | Visual acceptance harness | M4 | Gameplay Programmer | open | D-2/D-5 resolved (SD-METRIC). Bucket-occupancy coverage, 12-sector asymmetry, determinism, performance, silhouette break, four canonical screenshots. Records the parameter-resource content hash; a mismatched run is non-conforming. **Targets are the ratified AS-1 (≥70% overall / ≥90% sun-facing / ≥50% shaded) and AS-2 (≥20% by day 30, ≥40% by day 60, ≥30% at day 150), plus AS-3 clause (b): daily elongation within ±5% of a gate-disabled run.** |
| W-015 | Leaf and stem visual quality iteration pass | M4 | Gameplay Programmer | open | **M4 scope only** — M2.5 legibility minimum is W-060. Full remainder: SD-LEAF-1 cup, SD-LEAF-4 jitter/droop, SD-LEAF-5 full size model, SD-STEM taper/Catmull-Rom, placement-efficiency work deferred from SD-OPEN-6. Iterate to rubric ≥5/6 and clean blacklist. Scored against W-024 reference set (must be committed before rubric assessment). |
| W-017 | Coordinate-convention conformance tests | M1 | Code Reviewer | done | **Landed:** `src/core/conv.gd` plus `test/test_conv.gd` covering every SD-CONV row. Original spec: Unit tests for every SD-CONV row: gravity is `(0,−1,0)`; solar-noon sun is +Z with positive Y; tangent-basis fallback on a horizontal surface (the AS-6 path, SD-CONV-5/SD-EDGE-2); tower outward normals; light forward = −S. Cheap, and it prevents the single most expensive silent failure. |
| W-018 | Deterministic RNG stream service | M1 | Code Reviewer | done | **Landed:** `hash64.gd`, `rng_stream.gd` and `sim_clock.gd`, with `test_rng.gd` and `test_time.gd`. Determinism was nonetheless broken in practice by two later-found defects, W-033 (internal draws bound to the global RNG) and W-034 (the clock free-running on frame delta); both are fixed and regression-tested. Original spec: Per-tip event-advanced streams, `hash(parent_seed, branch_index)` for branches, fixed draw order, hashed field jitter (SD-RNG). Includes the AS-4 test and the SD-TIME-3 speed-change-invariance test. |
| W-019 | Static light precomputation bake | M2 | Code Reviewer | done | **Landed:** `light_bake.gd` bakes `SVF` and the 24-bit `V_hours` mask on the coarse grid and trilerps them up, keeps `P(cell,hour)` at full field resolution, and sits behind `invalidate(aabb)` as AR-FIELD-6 prescribed. Trilerped `SVF` had to be clamped to `[0,1]` — float error pushed it just over 1.0 and broke a test. Original spec: Hourly `P(cell,hour)` table, `SVF` hemisphere bake, 24-bit `V_hours` mask (SD-ENV-6). Behind `invalidate(aabb)` so Phase 2 mutability survives (SD-ENV-7, INV-9). This is what makes 20 ticks/s at Grow speed affordable for AS-5. **Architecture (AR-FIELD-6, AR-AMBIG-4):** 64 SVF rays + 24 visibility rays per cell over ~40k cells is ~3.5M GDScript raycasts — seconds of stall on every load. Bake the ray-based products (`SVF`, `V_hours`) on a coarse 0.12 m grid and trilerp them up; keep `P(cell,hour)` at full field resolution since it is pure arithmetic. Disk cache held in reserve, not built up front. |
| W-020 | Diel growth gate | M2 | Code Reviewer | done | **Landed:** `solar.diel_gate()` normalises the raw instantaneous elevation by its own 24-hour mean, and `physiology.gd` applies it to growth magnitude only. The gate-disabled path ships as `IvyParams.diel_gate_enabled`. **Note:** the AS-3 clause (b) comparison against that disabled run still cannot be executed — it needs the W-014 harness. Original spec: **Ratified 2026-08-09 as INV-3a** (SD-OPEN-1 closed; see the `DESIGN.md` ratification log). Mean-preserving instantaneous-elevation gate on growth magnitude only (SD-TIME-8). Must never appear in any directional term. Ship the gate-disabled path too — AS-3 clause (b) compares against it to prove mean-preservation. |
| W-021 | Coverage metric and light-field warm-up | M2 | Code Reviewer | done | **Landed 2026-08-09.** `src/metrics/coverage.gd` (`CoverageMetric`) implements SD-METRIC-1/2/3/5/6: 72 azimuth × 35 height bucket grid in tower-cylindrical coordinates, opening-exclusion mask (door + window, SD-METRIC-2), leaf-area threshold coverage with `\|dot(n_leaf, n_bucket)\|` weighting (AR-AMBIG-6), 12-sector stem-length and leaf-count split (SD-METRIC-6). Reports overall, sun-facing-180° and shaded-180° coverage per AS-1's three-number coupling rule. AS-2 stem-asymmetry computed from 12 sectors per seed azimuth. `test/test_metric.gd` added (29 new tests, 95/95 total). **Light-field warm-up (SD-TIME-7) was already wired**: `IvyEnvironment.warm_up()` called inside `build()` (env.gd:47), which fires from `sim_root.setup()` before the first tick — no change needed. `DUMP_METRICS` verb added to `tools/run_ui_script.gd`; `tools/ui_scripts/m2_metrics.txt` added. **Measured at default parameters, south seed:** AS-1 at day 150: overall=75.13% (≥70% ✓), sun-facing=93.83% (≥90% ✓), shaded=54.43% (≥50% ✓), lip reached ✓. AS-2 stem asymmetry: day 30=100.0% (≥20% ✓), day 60=97.86% (≥40% ✓), day 150=43.97% (≥30% ✓). AS-3(a): gate\_noon=2.07, gate\_midnight=0.114; midnight/noon=5.5% (≤10% ✓), midnight/daily-mean=11.4% (≤15% ✓). AS-3(b) mean-preservation confirmed in `test_time.gd`; full scripted comparison requires SET\_PARAM (W-027). M1 baseline unchanged at **2295 segs / 1027 leaves / 66.6238 m**, reproduced digit-for-digit (INV-7 intact). |
| W-022 | Artifact-blacklist assertions | M2 | Gameplay Programmer | open | Hard assertions for blacklist items 1, 2, 5, 6 in dev builds and the harness; numeric auto-screens for items 3 and 4 (directional-histogram 4-fold harmonic, leaf-density FFT, coplanar-pair scan). Item 7 stays human review with the SD-LEAF-6 adjacency guard (SD-METRIC D-5 table). |
| W-023 | Add `alpha_fill` to `leaf_atlas.json` | M2 | — | done | Closes SD-OPEN-4. `alpha_fill` (measured 0.607–0.675) and `tier` are present per leaf id in `assets/plants/ivy/LeafSet017/leaf_atlas.json`, so SD-METRIC-3 can compute projected leaf area analytically without rendering. W-021 is unblocked. |
| W-024 | Commit the three ivy-on-brick reference photographs | M4 | Game Director | open | **Gates M4 rubric scoring only — not M2.5.** Closes SD-OPEN-3. **Owner action required** (Director cannot download): create `assets/reference/ivy/` and commit three JPEGs plus `assets/CREDITS.md` entries. **Public Domain or CC0 only** — Wikimedia Commons; Unsplash, Pexels, CC-BY, CC-BY-SA excluded. If no acceptable image exists for a slot, replace that slot with a written descriptive criterion in `DESIGN.md` — never relax the license. **Slot requirements and search guidance:** **Slot 1** (`slot1_mat_edge.jpg`) — mid-range elevation of established ivy on brick/round masonry showing thick interior *and* sparse leading edge in one frame. Anchors rubric 2, 3. Commons search: `hedera heliv brick wall` + `license:cc0` or `license:public domain`; prefer cylindrical/curved masonry. Must show ≥30% brick and ≥30% ivy. **Slot 2** (`slot2_leaf_detail.jpg`) — close-range leaf mat: varied angles, overlap, self-shadow, specular highlight. Anchors rubric 4, 6. Search: `hedera helix leaves close` + PD/CC0; must show ≥10 individual leaves at readable scale. **Slot 3** (`slot3_silhouette.jpg`) — backlit/low-angle ivy breaking a roofline or wall edge against open sky. Anchors rubric 1, 5, AS-6. Search: `hedera roofline silhouette` or `ivy backlight wall` + PD/CC0. Each CREDITS.md entry: filename, source URL, author, license, retrieval date (2026-08-10 or later). **Acceptance:** three files exist, three CREDITS entries exist, Director confirms each slot's rubric anchors are exercisable. M2.5 LG-1 does not use these photographs. |
| W-025 | 3D composition root and render settings | M1 | Code Reviewer | done | **Landed:** `res://src/main/main.tscn` is now a `Node3D` composition root hosting World / Sim / PlantRender, with the scene path and UID preserved so both dev tools and `run/main_scene` still resolve. `msaa_3d = 2` and 4096 directional shadows are set in `project.godot`. **Still owed:** the four canonical cameras (tracked under W-002). Original spec: Added by the Gameplay Architect (AR-SCENE). Replace the bootstrap `Control` at `res://src/main/main.tscn` with a `Node3D` composition root hosting `World` / `Sim` / `PlantRender` / `UI`, **preserving the scene path and UID** so `tools/take_screenshot.gd`, `tools/run_ui_script.gd` and `project.godot`'s `run/main_scene` keep working — `tools/ui_scripts/smoke.txt` is the regression check. Apply the AR-SCENE-7 project settings: **`msaa_3d = 2`** (alpha-to-coverage is a silent no-op without MSAA), screen-space AA off (FXAA breaks AS-4 screenshot comparison), 4096 directional shadows. Four canonical cameras are authored transforms that are never written at runtime (AR-SCENE-3). |
| W-026 | Authoritative parameter resource | M1 | Code Reviewer | done | **Landed:** `ivy_params.gd` plus `ivy_params_default.tres`, injected from `Main` and never autoloaded, with derived values recomputed each tick and `content_hash()` exposed for the SD-METRIC integrity guard. `test_params.gd` guards the defaults. Original spec: Added by the Gameplay Architect (AR-PARAM). `IvyParams : Resource` holding every spec §30 default **and** every `SD-PARAM` row, plus `ivy_params_default.tres` (INV-6). Injected from `Main`, never autoloaded and never `load()`ed elsewhere. Derived values (`a = exp(−dt/τ_L)`, `gradient_epsilon`, `field_sample_jitter`) are stored as ratios and recomputed each tick, so retuning `field_cell` or `light_memory` cannot desync them (SD-ENV-8). Exposes `content_hash()` for the SD-METRIC harness-integrity guard. **First item in the whole queue** — build it late and §30 literals leak into call sites, which quietly kills G6. |
| W-027 | UI-script verbs for deterministic gameplay scripting | M3 | Gameplay Programmer | partial | **Landed:** `WAIT`, `SPEED`, `ADVANCE_DAYS`, `TRACE`, `DUMP`, `DUMP_LIGHT`, `DUMP_METRICS`, `SCREENSHOT`. `ADVANCE_DAYS` drives `SimClock` directly and, since W-034, scripted runs are pinned to exactly 24 ticks per game-day. `DUMP_METRICS [seed_azimuth_deg]` (added 2026-08-09, W-021) emits AS-1 coverage split, AS-2 stem asymmetry, AS-3(a) gate values, and total stem / leaf / segment counts. Output goes to `res://.tmp/`. **Still missing:** `CAMERA`, `SEED`, `SET_PARAM`, `ASSERT` — without `ASSERT` no M1–M4 visual gate can fail a script automatically. Original spec: Added by the Gameplay Architect (AR-UI-3). `tools/run_ui_script.gd` currently has only `WAIT` and `SCREENSHOT`, which cannot verify anything in this project. Add `CAMERA`, `SEED`, `SPEED`, **`ADVANCE_DAYS`**, `SET_PARAM`, `DUMP_METRICS`, `ASSERT`. `ADVANCE_DAYS` drives `SimClock.advance_ticks()` directly, so screenshot checks are reproducible and fast instead of wall-clock races — without it AS-4 cannot be verified from a script and every M1–M4 visual gate is flaky. |
| W-028 | Window mesh cull: carve the east window from the wall mesh | M1 | Gameplay Fixer | done | `tower_builder.gd` now splits rings in the window angular span into a below-sill strip and an above-window strip, mirroring the SDF subtraction. Regression test `test_tower_window_cull.gd` confirms no triangle spans across the window height band. `_spans_window` reuses `_angular_delta` for wraparound safety. Measured after fix: DUMP_LIGHT probe at E y=1.75 (inside the window opening, sill=1.60, win_top=2.20) reads |grad|=57.725 with a vertical-only component; the horizontal azimuthal component that caused the original 91.65 saturation dropped to ~0. The residual vertical gradient is the window-edge light discontinuity and is physically correct at that probe position. |
| W-029 | Stem hot chunk: flush partial chunk on every sync | M1 | Gameplay Fixer | done | `stem_mesh.gd` now calls `commit()` on the hot SurfaceTool and updates `_hot_instance.mesh` at the end of every `sync_from` call. The last 0–127 growing tip segments are now visible every frame. Regression test `test_stem_hot_flush.gd` confirms the mesh is non-null after a partial sync. |
| W-030 | SD-LEAF-4..8 leaf presentation richness | M4 | Gameplay Programmer | open | **M2.5 minimum moved to W-060** (SD-LEAF-6, -7, -5 `s_light`, -4 phototropic cant). **M4 remainder:** SD-LEAF-1 cup, SD-LEAF-4 full orientation/jitter/droop, SD-LEAF-5 full size model (`s_order`, `s_age`, `s_var`), SD-LEAF-3 phyllotaxy flattening if not already complete, AR-RENDER-3 static/growing split. Block on W-015 for rubric iteration. |
| W-031 | North `D_L` inflated near the doorway (projection artefact) | — | Systems Designer | open | Point probe at `(0, 1.75, -2.0)` reads ≈13.2 against SD-ENV-10 target ≈2.5; mean over north-facing cells is a healthy ≈4.2. Root cause: `project_to_shell` maps the doorway *exterior* onto the sunlit door *interior*, so the sample picks up lit-surface values. Needs a design decision on how openings are sampled before a fix can be written. Do not patch without resolving the projection semantics. |
| W-032 | Ivy does not climb: missing upward-bias term | — | Systems Designer | needs re-decision | `SVF = 0.500` and `D_L = 24.78` at every height on the south wall, so the light gradient is azimuthal-only — geometrically inevitable on a convex tower with no overhang. Spec §1 lists "upward bias approximating phototropism" but §21's direction-sum equation has no such term. A **negative-gravitropism** term was approved on that basis. **Re-open before implementing:** the evidence predates the W-033 determinism fix. Every observation of tips dying at ground level came from runs with a corrupted RNG. Against the reproducible baseline the plant climbs to ≈70% of tower height in 30 game-days, so the term may be unnecessary. **Baseline updated 2026-08-09 after W-036:** 2295 segments, 1027 leaves, 66.6238 m (was 2846 / 1385 / 82.16 before leaf crowding existed). Climb height is essentially unchanged, which strengthens the case that no upward-bias term is needed. Re-measure before changing the direction blend. |
| W-035 | East-face light gradient still saturates after the window cull | — | Systems Designer | open | W-028 removed the azimuthal component, but the probe at E y=1.75 still reads `\|grad\| = 57.7` against `light_gradient_scale = 4`. W-028 records this as a physically correct window-edge discontinuity, which is plausible for a probe sitting *inside* the opening — but any tip near the window edge still has `L` driven to a saturated unit vector, swamping persistence, adhesion and crowding. Decide whether openings need gradient clamping or a masked sample, and whether the probe location is representative. Shares root semantics with W-031. |
| W-033 | INV-7 violation: `RngStream` internal draws bound to the global RNG | M1 | Gameplay Fixer | done | `rand_unit_vector()` and `normal_std()` called `randf()` unqualified. That binds to the `@GlobalScope` built-in — the process-global RNG, seeded randomly at startup — not to `RngStream.randf()`. Every external call site writes `stream.randf()`, which is qualified and correct, so the only corrupted values were the growth-direction draws. Symptom: identical seeds produced 92 vs 4167 segments at day 30. Proof: with a fixed seed `u1 = 0.252041906118`, so `z` must be `0.495916`, but the function returned `-0.0228` then `0.7542` on consecutive runs. Fixed by qualifying with `self.`. Regression test in `test_rng.gd` asserts two identically seeded streams agree and that `z` derives from the stream's first draw. **Any method sharing a name with a `@GlobalScope` utility must be called with an explicit `self.`.** |
| W-034 | Scripted runs were not reproducible: clock free-ran on frame delta | M1 | Gameplay Fixer | done | `sim_root.setup()` started the clock at `Speed.GROW`, so `_process` advanced ticks from the real frame delta while a UI script was also driving `ADVANCE_DAYS`. Tick totals drifted with wall-clock timing (737 vs 736 for the same script). The clock now starts at `Speed.PAUSE`; `main.gd` starts it at Grow only when `script_driven` is false, and `tools/run_ui_script.gd` sets that flag before the scene enters the tree. Scripted runs are now pinned to exactly 24 ticks per game-day. |
| W-036 | SD-PHYS-3 / SD-LEAF-8 leaf crowding deposit entirely absent | M2 | Code Reviewer | done | **Landed 2026-08-09.** Two changes: (1) `physiology.gd` — added `deposit_leaf_crowding(pos, leaf_area, ctx)` which deposits `params.leaf_crowd_k * leaf_area / cell_area` (new param `leaf_crowd_k = 0.5`, added to `IvyParams` and `content_hash()`); called from `leaf_placer.gd` so physiology stays the sole writer (INV-1). (2) `leaf_placer.gd` — after node_count increment, samples crowding at node_pos and computes `P = clamp(1−leaf_crowd_suppress·C, leaf_crowd_floor, 1)`; suppression decision via `Hash64.unit_float(id, node_count, 42)` (hash-based, no stream draw, determinism preserved per SD-RNG-4). Regression tests: `test_leaf_deposit_raises_crowding_field` and `test_leaf_suppression_fires_at_high_crowding` in `test_leaf_placement.gd`. **New baseline: 2295 segments / 1027 leaves / 66.6238 m at day 30 (vs 2846 / 1385 / 82.16 before). Two back-to-back runs reproduced to the bit — INV-7 intact.** Delta: −19.4% segments, −25.8% leaves — expected modest reduction from crowding now suppressing growth. |
| W-037 | SD-TIP-4 vigour-retirement swap structurally unreachable | M2 | Code Reviewer | done | **Landed 2026-08-09.** Two changes to `tip_manager.gd`: (1) `can_branch()` changed from `live_count() < params.tip_cap_hard` to `return true` — `queue_branch()` already handles all internal gating (INV-1 comment matches reality now). (2) `branch_probability_scale()` changed to return `1.0` when `live_count() >= tip_cap_hard` instead of `0.0` — at the cap the full branch probability applies; the retirement margin in `queue_branch()` gates whether the swap actually happens. **Choice rationale:** making `can_branch()` return `true` reads most directly against SD-TIP-4 ("a new branch may still be created") and keeps `queue_branch()` as the single gating point for all SD-TIP-2/3/4 policy, rather than splitting logic between the call site and the function. Test `test_queue_branch_at_hard_cap_retires_weak_tip` re-pointed: asserts `can_branch(params)` is true at the cap before exercising the swap — this assertion would have failed before the fix. New test `test_branch_scale_at_hard_cap_is_full_for_sd_tip4` and `test_can_branch_true_at_hard_cap` added. |
| W-038 | SD-TIP-6 top-of-tower exemption absent from retirement candidate search | M2 | Code Reviewer | done | **Fixed 2026-08-09.** `TipManager` gains a `tower_height` field, defaulted from `TowerSpec.height` and set by `SimRoot` after the tower is built; `silhouette_height_frac = 0.8` and `silhouette_min_tips = 3` went into `IvyParams` and `content_hash()` rather than being hardcoded at the call site (INV-6). `_find_retirement_candidate(params)` now applies the three filters in the order AR-TIP documents: exclude FLOATING (SD-TIP-5), exclude tips above `silhouette_height_frac · tower_height` when fewer than `silhouette_min_tips` exist above it (SD-TIP-6), then the caller's vigour margin (SD-TIP-4). Height is injected as a field rather than a per-tip flag because the threshold is static between branch events. Two regression tests cover both directions — that a below-minimum set of silhouette tips survives even at vigour 0.001, and that the exemption lifts once the minimum is met. **This mattered more after W-040 than before it:** with the stall rule dead, retirement rarely fires at all, so the missing exemption was largely dormant; once W-040 lands and retirement becomes common, this guard is what keeps AS-6's silhouette tips alive. Original defect: `tip_manager.gd::_find_retirement_candidate()` had no check for SD-TIP-6 ("never retire a tip above `0.8 · H_tower` if fewer than 3 such tips exist") — `TowerSpec` height is not available to `TipManager` at all, so the check is structurally absent rather than merely wrong. Leaves the tips that produce the AS-6 silhouette break eligible for retirement at the hard cap. Needs tower height injected into `TipManager`, or the threshold stored per tip. |
| W-039 | `persistence_base` and `direction_memory` are dead §30 parameters (INV-6 / G6) | pre-M3 | Code Reviewer | **done** | **Fixed 2026-08-11.** `Physiology.w_P(h, params)` now returns `params.persistence_base * (0.7 + 0.3 * h)`, and `GrowthStep` blends `tip.direction * mem + d_actual * (1.0 - mem)` from `params.direction_memory`. Both are behaviour-preserving at the defaults, as the filing predicted, and the canonical day-150.25 run is bit-identical afterwards — 43,870 / 18,390 / 1288.816543 m, coverage 75.17 / 96.23 / 51.861702%, AS-2 51.31% — which is the evidence that the literals really did equal the defaults rather than merely looking as though they did. The `direction_memory` case is worth remembering: the old code carried **no literal at all**, since normalisation cancels the common factor in a 50/50 blend, so a scan for hardcoded defaults could never have found it. Guarded mechanically by the new `test_params_conformance.gd` (W-042), which fails on both halves when the fix is reverted. Original filing follows. | `physiology.gd::w_P` returns `0.5 * (0.7 + 0.3 * h)`, hardcoding the spec §12 outer coefficient rather than reading `params.persistence_base`. `growth_step.gd:63` does `tip.direction = (tip.direction + d_actual).normalized()`, hardcoding a 50/50 blend rather than `params.direction_memory`. Both names appear only in `ivy_params.gd` — the declaration and the `content_hash()` list — and nowhere else in `src/`. Results are correct at defaults because the literals happen to equal them, which is exactly what makes this dangerous: the dev overlay will appear to offer two tuning knobs that do nothing, and M3/M4 iteration would be steered by them. |
| W-040 | Stall rule and stall parameters are dead (INV-6) | M2 | Code Reviewer | done | **Fixed 2026-08-09.** `Tip` gains `stall_day_shoot`, `stall_day_last` and `stall_consecutive_days`; `TipManager._check_stall()` runs from `apply_lifecycle` at each game-day boundary for GROWING tips only, incrementing when elongation is below `stall_rate`, resetting on a good day, and setting DORMANT at `stall_days`. Plain floats and ints, no RNG draws, stray-random scan clean. Five regression tests; suite 109/109. **Day-30 is bit-identical to the M1 baseline** (2295 / 1027 / 66.6237528464408 m) as predicted. **W-040-only interim at day 150, reproduced twice: 40,952 segments / 17,728 leaves / 1202.53 m, AS-1 74.83 / 93.67 / 53.99 — all floors pass.** The rule fires (12 dormant at day 30, 112 at day 150) **but buys only ≈2% of the volume overshoot**: a trickle tip emitting one 0.03 m segment every three days still clears the 0.01 m/day threshold, so the tips this was meant to catch mostly escape it. That is the useful negative result — it rules out the stall rule as the main cause of the AR-BUDGET overshoot and is why the interim measurement was worth taking separately. Original defect: | `Tip` has no per-day elongation tracking fields and `tip_manager.gd::apply_lifecycle()` has no stall check, so SD-TIP's GROWING → DORMANT transition on "daily elongation < 0.01 m/day for 3 consecutive game-days" can never fire. `stall_rate` and `stall_days` appear only in `ivy_params.gd` and its `content_hash()` list. Consequence: zombie tips creeping along at saturation never retire and hold hard-cap slots indefinitely, which compounds W-037. |
| W-041 | `test_no_stray_random` scan has three false-negative holes | — | Gameplay Fixer | open | In `test_rng.gd`: (a) a file containing both an unqualified `randf()` and a qualified `stream.randf()` passes, because the test accepts any file that contains `stream.randf` anywhere; (b) the scan covers only `src/sim/`, `src/env/`, `src/world/` and `src/render/`, missing `src/core/`, `src/params/` and `src/main/` — `src/core/` is where W-033 actually lived; (c) an unqualified `randf_range(` does not match the `"randf("` needle. This is the guard against the single most expensive bug found so far and it would not have caught that bug. |
| W-087 | `read_trilinear` violated SD-EDGE-12: a uniform field produced a non-zero gradient | pre-M3 | Code Reviewer | **found and fixed 2026-08-11** | **Found by the new `test_field.gd` on its first run (W-042), which is the case for writing the guard rather than assuming the rule held.** `read_trilinear` returned `Σ(w·C) / Σw`; float addition is not associative, so two sample points 0.18 m apart produce different weight sets and land about one ULP either side of `C` even when every corner holds the same value. A central difference over a perfectly uniform field therefore reported a gradient of ≈6e-15, reaching a tip as ≈1.5e-15 of direction bias through the bounded form. Physically meaningless, but SD-EDGE-12 says *exactly* zero, and the reason it says so is that a uniform field must inject no phantom direction at all — a rule worth keeping true rather than nearly true, especially with M3 about to draw the field and its gradient on screen. **Fixed** by returning a uniform neighbourhood's value directly: interpolation across equal corners is the identity, so the general path was only ever adding rounding. **Deliberately not fixed** by the more general "interpolate deltas from a reference corner" restructuring, which would have changed the rounding of *every* field read and therefore re-based every canonical figure M2.5 had just closed on, for a 1.5e-15 improvement. Verified: canonical day-150.25 run unchanged — 43,870 / 18,390 / 1288.816543 m, coverage 75.17 / 96.23 / 51.861702%, AS-2 51.31%, LG-2′ 0.02239 / 0.0895 — so no re-ratification is needed. **Worth noting the class distinction:** today's platform-impossibility audit (SD-OPEN-12) covered requirements resting on properties the platform cannot guarantee, and correctly restated those. This is the mirror case — an exactness requirement that *is* obtainable in software, cheaply — so the code moved and the rule stood. SD-EDGE-12 was not in the audit table; the Director may want to record that the two classes are distinguished by whether the guarantee is purchasable, not by whether the requirement mentions exactness. |
| W-085 | AR-PARAM-3's §30 literal scan cannot work as specified — needs re-designing or withdrawing | pre-M4 | Systems Designer | open | **Measured 2026-08-11 while implementing W-042.** AR-PARAM-3 specifies a test that scans the model source for the numeric literals of the §30 defaults and fails on any hit outside `src/params/`, acknowledging it is "crude" and "will occasionally need an allowlist entry". Implemented as written it produced **dozens of hits, effectively all false positives**, because a bare float cannot be attributed to a parameter: `0.03` is both `segment_length` and `light_seek_min` and also an unrelated constant in `solar.gd`, `debug_camera.gd` and `leaf_colour_metric.gd`; `speed_watch`'s `60.0` matches every minutes-per-hour conversion in the codebase. Allowlisting each would silence the test into guarding nothing, which is the inert-guard class this project has now hit four times (the prose-only `base + gain` guardrail, W-039's dead knobs, three unfalsifiable W-076 tests, the SD-METRIC-3 projection). Worse, it **could not have caught W-039 anyway**: `persistence_base`'s `0.5` is indistinguishable from a midpoint and `direction_memory` had no literal at all. The reference scan shipped in `test_params_conformance.gd` catches that class properly, so INV-6 has a working guard and this is not urgent. The real question is a design one and belongs to the Systems Designer: **which numeric coefficients in the growth model are tunable parameters and which are spec-fixed formula constants** (§12's `0.7`/`0.3`, §14's `0.8`) — answer that and a scan for *any* unlisted literal in `src/sim/` becomes both possible and much stronger than the one specified. Until then AR-PARAM-3 is unimplemented by decision rather than by omission. |
| W-086 | Eleven declared §30 parameters have no implementation behind them | M4 | Systems Designer | open | **Found 2026-08-11 by the W-042 reference scan, which was written to catch exactly this and immediately found five times more than W-039 did.** Declared, hashed into `content_hash()`, and read by nothing: `droop_base`, `droop_shade_gain`, `leaf_expand_distance`, `leaf_jitter_roll`, `leaf_jitter_tilt`, `leaf_jitter_yaw`, `leaf_order_falloff`, `leaf_size_sigma`, `stem_order_falloff`, `stem_tip_taper`, `tip_cap_m1`. Ten are leaf and stem presentation the M4 visual pass owns — droop under gravity, per-leaf jitter, size variation, per-order thinning and taper — and `tip_cap_m1` is an M1 scaffold superseded by the SD-TIP soft/hard cap. **This is why W-039 mattered before M3 rather than after:** the dev overlay derives its controls from the exported list, so left alone it would present eleven sliders that move nothing, and any tuning session would draw false conclusions from them. Now enumerated and asserted in `test_params_conformance.gd`'s `UNIMPLEMENTED` list, which fails both if a new dead knob appears **and** if one of these is implemented without being removed, so the list cannot rot in either direction. **M3 requirement: the overlay must exclude this list or mark the controls plainly inert.** Decide per parameter at M4 whether to implement it or delete it; `tip_cap_m1` looks like a straight deletion, and deleting any of them changes `content_hash()`, so batch that decision. |
| W-042 | Ten planned test files do not exist — critical invariants have no guard | pre-M3 | Code Reviewer | **done (narrowed scope) 2026-08-11** | **Narrowed by owner decision to the invariants M3 will stress**, rather than writing all ten before the milestone: M3 mutates parameters at runtime and puts the environment field on screen. All three are written and the suite is **199/199**. `test_params_conformance.gd` (INV-6 dead knobs — 3 tests, mutation-verified against a reverted W-039) **found W-086**, eleven more dead knobs. `test_field.gd` (6 tests) **found W-087**, a real SD-EDGE-12 violation, on its first run. `test_determinism.gd` (AS-4 simulation half, 5 game-days) passes and is mutation-verified: swapping one `tip.stream.randf()` for a global `randf()` — the W-033 defect — fails all five assertions at 18 vs 28 segments. **Two of the three guards found a live defect the moment they existed**, which is the argument for having written them before M3 rather than after, and a caution about the six still deferred. **One known fragility:** `test_determinism.gd` reimplements `SimRoot._tick()`'s order of operations rather than calling it, because the tick loop is not exposed independently of the scene tree. The duplication is documented in the test with the reason the ordering matters (`apply_lifecycle` reads `tick_index` before the clock advances, which is what makes the W-040 stall rule correct), but it can drift: a change to the real tick order would leave this test passing against a stale copy. Extracting a headless-callable tick from `SimRoot` would fix it properly and is worth doing when M3 touches that file anyway. Filed as a note here rather than its own row because it is a test-design debt, not a defect. Deferred with no milestone claim: `test_physiology.gd`, `test_geometry.gd`, `test_surface_query.gd`, `test_conv_light.gd`, `test_tower_geometry.gd`, `test_blacklist.gd`. **The filing's own list is stale in two ways:** `test_metric.gd` exists and has since W-021 (it now carries the W-076 invariance guards and the W-079 orientation guard), and `test_params_conformance.gd`'s specified literal scan turned out not to be implementable as written — see W-085. Original filing follows. | `TECHNICAL_INDEX.md` lists these as planned; none are written: `test_physiology.gd`, `test_geometry.gd`, `test_determinism.gd` (AS-4), `test_field.gd` (trilinear / gradient / shell), `test_surface_query.gd`, `test_params_conformance.gd` (INV-6 literal scan), `test_conv_light.gd` (SD-CONV-9), `test_tower_geometry.gd`, `test_metric.gd`, `test_blacklist.gd`. The 63 passing tests therefore say less than they appear to: AS-4 determinism, the INV-6 literal scan and every acceptance metric have zero automated coverage. `test_params_conformance.gd` would have caught W-039 and W-040 mechanically. |
| W-043 | `tip.direction` silently zeroes on antiparallel reflection | — | Gameplay Fixer | open | `growth_step.gd:63`: if `tip.direction` and `d_actual` are exactly antiparallel — a near-perfect mirror bounce at a wall crease — the sum is `Vector3.ZERO` and Godot's `Vector3.ZERO.normalized()` returns `(0,0,0)`, permanently zeroing that tip's persistence term for the rest of the run. Low probability, but silent and unrecoverable. Add the same `length_squared() < 1e-8` guard already used for the direction sum on line 35. |
| W-044 | `surface_query.nearest()` hardcodes `BRICK_WALL` — INV-10 only partially met | — | Gameplay Fixer | open | `surface_query.gd:69` sets `result.material_id = MaterialRegistry.BRICK_WALL` unconditionally, while the raycast path at line 49 correctly reads `material_at_face(result.face_index)`. `growth_step.gd` calls `adhesion_suitability(near.material_id)` on the nearest-path result, so adhesion on that path is effectively hardcoded. Harmless in Phase 1 because every surface is brick, which is precisely why it will not surface until a second material is introduced. |
| W-045 | W-037's fix makes `branch_probability_scale` discontinuous at the hard cap, against SD-TIP-3 | M2 | Code Reviewer | done | **Fixed 2026-08-09.** `branch_probability_scale()` is now the single continuous ramp the Systems Designer specified: `q = branch_scale_floor + (1 − branch_scale_floor) · clamp((N_hard − N)/(N_hard − N_soft), 0, 1)`, giving `q = 1.0` below the soft cap, falling linearly to `branch_scale_floor` at the hard cap and held there above it. `branch_scale_floor : float = 0.02` added to `IvyParams` and `content_hash()` (INV-6). The new `test_branch_scale_continuity_no_pop` sweeps the whole range and asserts the largest adjacent step is `(1 − floor)/(N_hard − N_soft) ≈ 0.0153` — the property that would have caught the original defect, rather than a spot check at the cap. Two existing tests were re-pointed rather than deleted. **Day-30 baseline unchanged at 2295 / 1027 / 66.6237528464408 m**, byte-identical across two runs, as predicted: that run peaks at 67 live tips and never enters the taper. **Day-150 moved to 41,763 segments / 18,107 leaves / 1226.90 m** (from 42,795 / 18,488 / 1257.93), with AS-1 at 76.89 / 94.71 / 57.18 — the small reduction is the expected effect of churn at saturation being throttled to 2% of the unthrottled rate. ⚠️ **The day-150 figure is from a single run and is not yet confirmed:** the Fixer's second run was blocked, and an independent re-run was attempted but hung on contention with another agent's concurrent Godot process. Re-measure before relying on it. Original defect: introduced by the W-037 fix and found in post-fix verification. `tip_manager.gd::branch_probability_scale()` now tapers `q = (N_hard − N)/(N_hard − N_soft)` from 1.0 at 96 live tips down to **1/64 ≈ 0.016 at 159**, then returns **1.0 at 160**. That is a 64× jump at a single tip, and SD-TIP-3 states the taper goes "smoothly to zero. **No pop when the cap is approached.**" The fix was necessary — with `q = 0` at the cap the branch draw never fires and SD-TIP-4's swap is unreachable, which was the W-037 defect — but the chosen value reintroduces the pop from the other side. Predicted dynamics: the population is throttled hard as it approaches 160, then churns at full branch rate once it arrives, retiring a tip per branch. Because SD-TIP-4 retires the least-vigorous tip and shaded tips are least vigorous, full-rate churn at the cap maximises exactly the mechanism `DESIGN.md` flags as threatening AS-1's 50% shaded floor, and it compounds W-038 (the missing SD-TIP-6 silhouette exemption). **Currently unobserved:** the day-30 run peaks at 67 live tips, well below the 96 soft cap, so nothing in the M1 script or the test suite exercises this region — it will first appear in the long M2/M4 runs where AS-1 is measured. Needs a Systems Designer decision on the branch rate at the cap; a continuous formulation (taper to a floor `q_min` and use that same `q_min` at and above the cap) would satisfy both SD-TIP-3 and SD-TIP-4, but the floor value is a design choice, not a fix. **Systems Designer decision (2026-08-09, → Gameplay Fixer).** Replace the discontinuous taper with one continuous piecewise ramp to a positive floor. Add param `branch_scale_floor : float = 0.02` to `IvyParams` and its `content_hash()` list (INV-6). Amended SD-TIP-3/4: `q = branch_scale_floor + (1 − branch_scale_floor)·clamp((N_hard − N)/(N_hard − N_soft), 0, 1)`, giving `q = 1.0` for `N ≤ N_soft`, a linear fall to `q = branch_scale_floor` at `N = N_hard`, and `q = branch_scale_floor` held for all `N ≥ N_hard`. C0-continuous — the 64× pop is gone (largest adjacent-N step ≈ (1−floor)/64 ≈ 0.0153) — while `q(N_hard) = 0.02 > 0` keeps SD-TIP-4's retirement swap reachable (the W-037 requirement). **Intended steady state at saturation:** slow churn, at most one retirement per *successful* branch, throttled to ~2% of the unthrottled branch rate — not the full-rate one-in-one-out the old `1.0`-at-cap produced. **Default 0.02** is a low first estimate chosen to minimise shaded-tip retirement and protect AS-1's 50% floor; it is orthogonal to `retire_margin` (floor sets *how often* a swap is attempted, margin sets *which* tip is retired), which stays the designated lever per `DESIGN.md` if the shaded floor is still threatened. Tune against the W-021 coverage metric once the cap is actually reached — do not pre-tune. **W-038 is a prerequisite for AS-6 at saturation** (any nonzero churn can retire silhouette tips until SD-TIP-6 is enforced), though not a prerequisite for landing this fix. **Cap reachability:** unobserved today (67 live tips at day 30, below the 96 soft cap), but likely reached over a 150-day run — and pushed higher while the stall rule stays dead (W-040 leaves zombie tips holding slots). No change recommended to `tip_cap_soft`/`tip_cap_hard`. |
| W-046 | Tower renders with no visible brick pattern | M2.5 | Gameplay Fixer | done | **Closed 2026-08-10.** Delivered in two passes: cylindrical UV generation added to `tower_builder.gd` (the mesh previously had no UVs at all, so the fully-wired Bricks094 PBR material could not tile), then corrected under W-065 after the first pass shipped a 2.07× aspect distortion. The tower now shows horizontal brick coursing at true real-world scale. Suite 118 → 128; day-150 numbers bit-identical throughout, since UVs are render-only. Follow-ups: W-066 (test tying UV scale to the material's physical size) landed with the fix; W-067 (stale screenshot captures, found while verifying this) remains open. **Original diagnosis below.** | **AR-TOWER-1 — Gameplay Programmer fixing now.** Root cause: `tower_builder.gd` never calls `set_uv`; `TowerSpec.brick_uv_metres` unread. LG-4 requires repeating brick detail visible on sun-facing elevation. Exclude from M2.5 sequencing order — lands imminently and changes every subsequent screenshot. |
| W-047 | AS-3 clauses (b) and (c) are unmeasured — M2's gate cannot be declared | M2 | Code Reviewer | done | **Landed 2026-08-09.** `SET_PARAM` added to `tools/run_ui_script.gd` (also clears part of W-027 and unblocks M3 live tuning), plus scripts `as3b_gate_on.txt`, `as3b_nodiel.txt` and `as3c_dl_decay.txt`, and 9 new tests in `test_time.gd`. **AS-3(c) passes cleanly:** measured `tau_L_implied = 3.0000` days against a target of 3.0, with the step response decaying to 0.3679 after three days of zero input — exactly `exp(−1)`. **AS-3(b) needs care and is written up as W-049.** The literal criterion fails: day 29→30 elongation is 6.267 m with the gate on against 5.747 m with it off, a 9.05% gap against a ±5% tolerance. But that comparison is confounded, because by day 29 the two runs are different plants (60.357 m vs 58.688 m of stem) and a larger plant elongates more regardless of the gate. Measuring from an identical starting state instead shows the gate *is* mean-preserving: cumulative stem length differs by **0.028% at day 1** and **0.052% at day 3**, and even at day 29 the cumulative gap is only **2.84%** — inside tolerance. Single-day deltas swing wildly in both directions over the same interval (−9.98% on day 1→2, +6.75% on day 2→3) because per-metre branch and node events are discrete and fire at slightly different times. So the gate is sound and the criterion is the problem. Original scope: W-021 measured AS-3 clause (a) only (midnight rate 11.4% of daily mean, 5.5% of noon — both pass). Clause **(b)**, total elongation over a whole game-day within ±5% of the same run with the diel gate disabled, has no automation: `tools/ui_scripts/m2_metrics_nodiel.txt` exists but its own header says to "run headlessly with a custom params resource… or compare manually". Clause **(c)**, `D_L` lagging a step change in instantaneous light with a ~3-game-day time constant, is not measured at all. Clause (b) is the one `DESIGN.md` calls out as protecting AS-1 — a gate that quietly halved total growth would pass (a) and (c) and fail AS-1 only much later. Needs a params-override path for scripted runs (or a `SET_PARAM` verb, already owed by W-027) so the gate-disabled comparison runs unattended, plus a step-change probe for (c). **M2's exit condition is "AS-2 and AS-3 pass"; until this lands the gate is open regardless of the other numbers.** |
| W-048 | Day-150 run sits ~2× outside the `AR-BUDGET` envelope — AS-1 is passing by overgrowth | M2 | QA Playtester | done | **Director ruling (SD-OPEN-6, `DESIGN.md` ratification log):** split into two concerns. **Volume half — closed:** AR-BUDGET re-derived upward (35–45k segments / 1,000–1,400 m / 55–70 m² / 850–1,050 tips ever created); measured run (40,952 / 1,202 m / ≈60 m² / 962 tips) is now inside the envelope. Placement-efficiency improvement deferred to M4 (W-015/W-030). Volume brakes rejected. AS-1 floors unchanged. **Visual half — landed 2026-08-09 (Gameplay Fixer):** SD-LEAF-8 levers applied: `leaf_crowd_suppress` 0.55→0.70, `leaf_crowd_floor` 0.35→0.30. Measured day-150 (two back-to-back runs, digit-for-digit): 43,743 segs / 18,339 leaves / 1,285.07 m; AS-1 = **74.58 / 96.23 / 50.62** — all floors pass. ⚠️ **Shaded floor 50.62% is 0.62 points above the 50% floor — within 1 point.** AS-2 asymmetry 51.53% (unchanged). Segment count increased to 43,743 as predicted (thinner leaf deposit eases `f_C` and `λ_b`). Green mat: individual leaves and layering now legible on the sunny face; no longer reads as uniform wallpaper. INV-7 intact. Suite: 109/109. Pending: QA Playtester verification of all M2 criteria. |
| W-049 | AS-3(b)'s "any whole game-day" wording is not a robust test of mean-preservation | M2 | Game Director | **superseded 2026-08-09 (SD-OPEN-7)** | **Director ratification (first restatement):** AS-3(b) reworded in `DESIGN.md` to test *cumulative* stem elongation from game-day 0 through day N (N ≥ 1) against the same seed with the diel gate disabled, within ±5%. Single-day deltas are explicitly not the test instrument. Evidence (W-047): cumulative gap is 0.028% at day 1, 0.052% at day 3, 2.84% at day 29 — all inside tolerance. **Superseded by SD-OPEN-7:** branched cumulative comparison withdrawn; see W-052 ratification log. |
| W-050 | Crowding is hard-clamped to [0, 1], so `leaf_crowd_k` cannot deepen saturation — it only thins sparse regions | M2 | done | **consumed by the W-048 re-decision 2026-08-09; closed** | **Resolution 2026-08-09:** this finding drove the W-048 re-decision (see that row). Settled: (1) the `C ∈ [0,1]` clamp **stays** — it is what keeps `λ_b`'s `pow(1−C, 1.5)` off a negative base, and lifting the ceiling would require a matching edit to both `f_C` and that `λ_b` term, which is out of scope. (2) `leaf_crowd_k` is confirmed a saturation-*rate* knob and is **held at 0.5** — do not re-raise it; recorded in SD-LEAF-8 and the SD-PARAM note so it is not re-tried. (3) `leaf_crowd_suppress`/`leaf_crowd_floor` are the density-gated levers that act *through* `C` and spare the sparse shaded half; the re-decision uses them (0.55→0.70 / 0.35→0.30 pending). (4) The recommendation to reach for `branch_rate` was examined and **rejected** — the SD-TIP-3 taper is a homeostat that makes `branch_rate` inert until ~1.0 and then collapses the shaded half first (see W-048 row); the underlying budget-vs-shaded-floor conflict is escalated as SD-OPEN-6. Original finding retained below. — Found while reviewing why W-048's tuning produced 92.47% sun / 30.76% shade. `Environment.deposit_crowding()` (`src/env/environment.gd:116`) writes via `SparseHashField.add_slot(CROWDING, slot, amount, 0.0, 1.0)`, and `add_slot` is `arr[slot] = clampf(arr[slot] + delta, lo, hi)`. **C is therefore structurally confined to [0, 1]**, whatever `leaf_crowd_k` is set to. This makes `leaf_crowd_k` behave in the opposite way to how W-048 reasoned about it. In an already-saturated cell C is pinned at 1 and raising `k` changes nothing at all; the only cells whose C actually moves are those still below saturation. Raising `k` is thus not a "make dense areas denser-suppressed" lever but a "make sparse areas reach full suppression sooner" lever — and on this tower the sparse cells are precisely the shaded half that AS-1's 50% floor protects. That is the whole explanation for the measured split: the sunny side barely moved (93.67 → 92.47) because it was already clamped, and the shaded side collapsed (53.99 → 30.76) because it was not. It also explains why volume barely moved: most of the plant lives in cells that were already at C = 1, so the tuning could not reach them. **Consequences to settle before re-deciding W-048.** (1) Whether the [0, 1] clamp is intended. `f_C = e^{−0.8C}` and `lambda_b`'s `pow(1 − C, 1.5)` both behave sensibly on [0, 1] — and the clamp is what keeps that `pow` off a negative base, so removing the clamp naively would produce NaN branch probabilities and must not be done without changing `lambda_b` too. (2) If the clamp stays, C saturates and stops discriminating, so *no* crowding parameter can distinguish "one leaf layer" from "three", which is exactly the discrimination the AR-BUDGET overshoot needs; the volume lever then has to be branching or retirement, not crowding. (3) `leaf_crowd_suppress` and `leaf_crowd_floor` still work as intended on saturated cells and were not the problem. **Recommendation for the re-decision, not a decision:** treat `leaf_crowd_k` as a saturation-*rate* knob to be left at or below its old 0.5, address the 2× volume overshoot through `branch_rate` (the measurement points at branching — 962 tips created against a 300–450 budget, while stem length per tip at 1.2 m is on target), and, if C needs to discriminate beyond saturation, raise the clamp ceiling deliberately with a matching change to `lambda_b`'s crowding term. |
| W-051 | Every checkpoint screenshot was captured at 06:00, so no visual acceptance judgement made from `m2_metrics.txt` was reliable | M2 | Code Reviewer | done | **Found and fixed 2026-08-09** while verifying the W-048 visual verdict. `start_hour` is 6.0 and `day_of_year` is fixed at 105 (it does not advance with the simulation), so `ADVANCE_DAYS` by any whole number of days always lands at **06:00 with the sun a few degrees above the horizon**. The day-150 frame is a dim, flat, low-contrast image in which the plant reads as a uniform dark green mass — no leaf shapes, no exposed wall, no legible sun/shade asymmetry. **Two consecutive Fixer cycles made their visual assessments from that frame**, one reporting "large areas of exposed brick on the north side" and the next "individual leaf shapes distinguishable, visible texture variation, visual depth". Re-rendering the identical simulation state six game-hours later at local noon shows the second description is essentially correct and the first was reading darkness as bare wall — but neither was supportable from the image it was based on, and the Director had made "is the green mat gone" an M2 blocker resting on exactly that judgement. **Fix:** `m2_metrics.txt` now advances `0.25` days before each `SCREENSHOT` and `0.75` after it, so screenshots land at local noon while the metric dumps stay on the same tick counts as before — 720 / 1440 / 3600. Verified: all three checkpoints reproduce digit-for-digit against the pre-change run (day-150 43,743 / 18,339 / 1285.07008928194 m, AS-1 74.58 / 96.23 / 50.62), confirming the offset does not perturb the trajectory. Metrics were never affected either way, since `CoverageMetric` reads the plant and the field rather than pixels. **Lesson for future stages:** a screenshot is only evidence if you know what time of day it was taken. The four canonical cameras still owed by W-002 should each pin a time of day as well as a transform. **QA 2026-08-09:** confirmed — midday `m2_day150.png` is legible; `as3b_day30_gate_on.png` (no noon offset) is inadmissible for visual judgement. |
| W-052 | AS-3(b) cumulative mean-preservation fails at day 29+ under current defaults | M2 | Game Director | **ratified 2026-08-09 (SD-OPEN-7)** | **Director ratification:** W-049 branched cumulative comparison withdrawn — it measured Lyapunov divergence, not the gate (0.39% gap with `branch_rate = 0` vs 11% with branching; sign reversed under unrelated parameter changes). AS-3(b) reworded in `DESIGN.md` per `IMPLEMENTATION.md` § SD-TIME-8f: (b.1) unit invariant `mean(ĝ) = 1` over 24 tick-hours to 1×10⁻⁶; (b.2) near-linear applied backstop ±2% at game-day 29 with branching disabled (measured 0.39%). Division of labour accepted: AS-3(b) = budget guard only; directional misapplication delegated to AS-3(a) + INV-3a. Acceptance-signal audit accepted; AS-4 guardrail written into `DESIGN.md`. M2 gate declared done. **Follow-up:** W-053 (Gameplay Fixer) — tighten `test_diel_gate_mean_is_unity_over_24_ticks` tolerance 0.02 → 1e-6; implement INV-3a directional-term guard listed in test surface. QA lightweight re-verify of AS-3(b) instruments after W-053. — Original QA filing and Systems Designer proposal retained below. — **Systems Designer proposal 2026-08-09 (→ Game Director).** Full wording, rationale, and audit in `IMPLEMENTATION.md` § SD-TIME-8f and SD-OPEN-7. **Restatement:** retire the W-049 branched cumulative comparison — with branching enabled it measures the simulation's Lyapunov divergence (day-29 gap 11%), not the gate, and is unsatisfiable at large N for any nonlinear growth model. Re-pin AS-3(b) on two non-chaotic checks: (1) **unit invariant** `mean(ĝ) = 1` over the 24 tick-hours, exact by construction — strengthen `test_time.gd::test_diel_gate_mean_is_unity_over_24_ticks` tolerance from `0.02` to `1e-6` (±0.02 would pass a 1.9%-off gate); (2) **near-linear applied backstop** — with `SET_PARAM branch_rate 0.0` in both runs, cumulative `TOTAL_STEM_LENGTH` gate-on within **±2%** of gate-off at game-day 29 (measured 0.39% → ≈5× margin; scripts `qa_as3b_nobranch_on.txt` / `qa_as3b_nobranch_off.txt`). **Catches:** a gate that scales the daily budget by any constant ≠ 1 (fails both checks, ~50% gap and mean 0.5). **Does not catch, by design:** a gate applied to a directional term — no elongation comparison can see an un-gated magnitude; delegated to **AS-3(a)** (night rate would not fall) + INV-3a review. **Executable today** (`SET_PARAM`/`ADVANCE_DAYS`/`DUMP_METRICS` all exist); **not gated on W-027** — comparing two dumped numbers by hand or via the W-014 harness reaches a reproducible verdict without `ASSERT`/`SEED`. **Class-defect audit:** AS-4 is immune *only because* it demands bit-identity between runs with zero input difference — guardrail: never relax it to a tolerance; AS-5 immune (single-run thresholds); AS-2's shade-seed-vs-sun-seed clause is a two-run comparison but an inequality-with-margin, so far less exposed — low-severity watch near the 20% threshold, no action now. **Escalation:** AS-3 and INV-3a are Director-owned; ratify or reject on merits, same route as W-049. — **Reclassified on review 2026-08-09, after QA filed this as a Fixer blocker. There is no defect in the diel gate and there is nothing for a Fixer to fix.** The decisive test isolates the gate from the branch cascade, which is the compounding amplifier in this system — every branch adds a tip that itself grows and branches, so any timing difference is amplified exponentially. Re-running the gate-on/gate-off pair with `SET_PARAM branch_rate 0.0` in both runs (scripts `qa_as3b_nobranch_on.txt` / `qa_as3b_nobranch_off.txt`): at day 29 gate-on measures **1.4183782376349 m** against gate-off **1.41293515264988 m**, a gap of **0.39%** — comfortably inside ±5%, where the same comparison with branching enabled gives 11%. The 11% is therefore produced entirely by branch amplification of sub-millimetre timing differences, not by any bias in the gate. Two further pieces of evidence agree. **The sign flip:** at W-049 ratification the day-29 gap was 2.84% with the gated run *ahead*; it is now 11% with the gated run *behind*. A systematic bias in a mean-preserving multiplier does not reverse direction when leaf-crowding parameters change — reversal under unrelated parameter changes is the signature of trajectory divergence in a chaotic system. **The property is already pinned at the source:** `test_time.gd::test_diel_gate_mean_is_unity_over_24_ticks` asserts the gate multiplier sums to exactly 24 over 24 ticks, i.e. mean exactly 1. Mean-preservation is guaranteed by construction and unit-tested; the plant-level comparison adds no coverage of it. **What this means for the criterion.** W-049 identified two confounds and the ratified restatement only fixed one. Moving from single-day deltas to cumulative totals removed the discretisation noise (confound 2) but not the fact that the two runs stop being the same plant after tick 0 (confound 1). Cumulative comparison merely relocates the noise floor: it is tight at day 1 (0.028%) and day 3 (0.051%) because divergence has not yet compounded, and it will exceed any fixed tolerance at some N for any nonlinear growth model, no matter how correct the gate. **As ratified, AS-3(b) is unsatisfiable at large N by construction, and passing it would require making the simulation less nonlinear rather than making the gate more correct.** Candidate restatements that isolate the intended property: (i) compare with `branch_rate = 0`, the near-linear regime measured above; (ii) compare cumulative totals over a short window such as day 0→1 or 0→3, before amplification; (iii) drop the plant-level comparison and rest the criterion on the existing unit test plus AS-3(a) and (c), which both pass. **AS-3 and INV-3a are Director-owned, so this needs a Systems Designer proposal and Director ratification — the same route as W-049, which is precisely why it should not be handed to a Fixer.** Until it is settled, M2's gate cannot be declared either way. QA's original filing follows, and its measurements are all reproducible and correct — only the attribution changes. — **Found QA 2026-08-09 — M2 blocker.** Context: default south seed, Godot 4.4.1, branch after W-048 SD-LEAF-8 levers (0.70/0.30), W-040 stall rule, W-045 branch_scale_floor. Reproduction: run `as3b_gate_on.txt` and `as3b_nodiel.txt` (`SET_PARAM diel_gate_enabled false` before first tick). Compare `TOTAL_STEM_LENGTH` from `DUMP_METRICS`. Expected (W-049 ratified AS-3(b)): cumulative stem length gate-on vs gate-off within ±5% for all N ≥ 1. Actual: day 1 gap 0.028% ✓, day 3 gap 0.051% ✓, **day 29 gate-on 57.942912 m vs gate-off 64.336520 m → 11.03% gap ✗**, day 30 gate-on 63.267215 m vs gate-off 70.599620 m → 11.59% gap ✗. Gate-off run produces more total stem — opposite of W-047's pre-W-048 2.84% measurement, suggesting post-Fixer parameter changes amplified discrete-event divergence beyond tolerance. Severity: **blocker** (M2 gate is AS-2 and AS-3 pass). Affected: `src/env/solar.gd` (`diel_gate`), `src/sim/physiology.gd` (gate application), possibly interaction with stall rule / branch timing. Scripts: `tools/ui_scripts/as3b_gate_on.txt`, `as3b_nodiel.txt`, `as3b_day1_*.txt`. Do not re-tune to satisfy single-day deltas (W-049). |
| W-053 | AS-3(b) unit tolerance + INV-3a directional guard (SD-OPEN-7 follow-up) | M2→M3 | — | done | **Fixed 2026-08-09.** (1) Tolerance tightened to `1e-6` in `test_diel_gate_mean_is_unity_over_24_ticks`, with a comment recording why a loose epsilon is wrong here: `g_ref` is the mean of `g` over the very same 24 hours, so the result is exactly 1 by construction and any slack is slack in AS-3(b) itself. (2) The INV-3a directional guarantee is now asserted two ways rather than one. `test_diel_gate_scales_growth_rate_proportionally` checks that doubling `ĝ` exactly doubles `Physiology.growth_rate`, which fails if the gate ever leaks into a saturating or directional term; `test_diel_gate_never_reaches_direction_code` scans `growth_step.gd` and `tip_manager.gd` for `diel_gate` / `g_hat`, mirroring the existing stray-random scan, and catches the regression at the point someone introduces it rather than after it changes behaviour. Suite **111/111**. **Both new guards were mutation-tested rather than assumed:** appending a comment containing `g_hat` to `growth_step.gd` fails the scan, and changing the application site to `r *= sqrt(g_hat)` fails the proportionality check (0.1697 against an expected 0.24). Both mutations were reverted. This matters because the defect being fixed here *was* a test that could not fail — asserting a construction-exact quantity to ±0.02 — so shipping replacements without checking they bite would have repeated it. Original ruling: **Director ruling 2026-08-09 (SD-OPEN-7).** Two mechanical test gaps, neither blocking M2 ship: (1) tighten `test_time.gd::test_diel_gate_mean_is_unity_over_24_ticks` tolerance from `0.02` to `1e-6` per ratified AS-3(b.1) — current ±0.02 would pass a gate 1.9% off mean-preservation; (2) implement the "gate value never reaches any directional term" assertion listed in `IMPLEMENTATION.md` test surface but absent from `test_time.gd` — an INV-3a guard that AS-3(b) explicitly does not cover. Must land before M3 tuning work. QA lightweight re-verify of AS-3(b) instruments after landing. |
| W-054 | Interactive debug orbit/zoom camera (developer affordance) | — | Code Reviewer | done | **Implemented and reviewed 2026-08-10; closed with no Fixer pass.** `src/world/debug_camera.gd` (`DebugCamera : Camera3D`) + `Main/World/DebugCamera` in `main.tscn` + 4 tests in `test/test_debug_camera.gd`. Suite 111→115. **Isolation verified:** all writes to `current`/`global_transform` are structurally confined to `_unhandled_input`, so the `take_screenshot.gd` path (which does *not* set `script_driven`) is protected by structure rather than by the flag; `smoke.txt` screenshot byte-identical before/after; day-150 numbers unchanged at 43,743 segs / 18,339 leaves / 1285.07008928194 m, AS-1 74.58 / 96.23 / 50.62. Test 3 confirmed a genuine mutation guard (goes red when the `script_driven` early-return is removed), and the forbidden W-053-style "simulated drag moves the camera" test was correctly not written. Follow-ups filed as W-055 and W-056; neither blocks. **Original build spec below.** Add mouse-wheel zoom and click-drag orbit so a developer pressing Play can inspect the tower — specifically the shaded half, which `CamSun` (due south, at the sunlit seed face) never shows. **Scope is deliberately tiny: one `Camera3D`, one script, three state floats, one `_unhandled_input`. Not a camera framework** — no input-map actions, config resource, easing, or settings UI (bounce back to the Architect if it grows). **Build:** new node `Main/World/DebugCamera` (`Camera3D`, authored `current = false`), a **sibling of `CameraRig`, not a child**, script `src/world/debug_camera.gd` `class_name DebugCamera`. Add both to `main.tscn`. **Pivot** = `Vector3(0, TowerSpec.height * 0.5, 0)` = `(0, 1.75, 0)`, derived from the injected `TowerSpec`, never hardcoded (AR-DBGCAM-3). **Bounds** (all from `TowerSpec`): radius clamp `[radius_outer + 1.0, (height + 2·radius_outer)·1.6]` = `[3.0, 12.0] m`; pitch clamp `[-5°, +85°]`; yaw unbounded. Multiplicative wheel step, fixed pixels→degrees drag (AR-DBGCAM-4). **Default camera:** `CamSun` stays authored `current = true`; `DebugCamera` opts in on the **first** wheel/drag event and seeds `yaw/pitch/radius` from `CamSun`'s transform at `_ready` so activation has no visual jump (AR-DBGCAM-2). **Scripted-run isolation (the load-bearing requirement):** write `current`/transform **only** inside `_unhandled_input` (no automated runner injects input, so `CamSun` stays current in every screenshot/GUT run — this is what protects AS-4), **plus** `set_process_unhandled_input(false)` when `script_driven` is true, injected from `Main` (AR-DBGCAM-5). **Tests (AR-DBGCAM-6):** factor transform math into a pure helper and GUT-test clamps + that the camera never goes underground or inside the wall at the four bounds corners + that a synthetic input under `script_driven` leaves `current` false and the transform unchanged. **Do not** write a "simulated drag moves the camera" test that passes with a stubbed handler (the W-053 could-not-fail trap). The interactive feel is verified by a human pressing Play; the automated check is `tools/ui_scripts/smoke.txt` still framing `CamSun` byte-identically (the negative/isolation proof — a `run-ui-script` scenario cannot and must not drive this affordance). **Non-goals:** does not add or substitute for the three canonical cameras still owed by W-002; does not touch `CameraRig`; introduces no RNG (determinism scan must stay clean). Next stage after implementation: **Code Reviewer** (verify the AR-DBGCAM-5 checklist), then Gameplay Fixer if needed, then QA. |
| W-055 | `DebugCamera.spec` is loaded independently rather than sourced from `World.tower_spec` | — | Gameplay Fixer | open | **Found by Code Reviewer 2026-08-10 reviewing W-054. Latent, not live — deferred deliberately.** `debug_camera.gd` falls back to `load("res://src/world/tower_spec_default.tres")` and `world.gd:16` loads the same path; Godot caches by path, so both hold the *same* instance today and the orbit pivot is correct. The risk is silent divergence if `World` ever sources its spec from anywhere else — the camera would then orbit a pivot that is not the tower's centre, with no error. **Note for the Fixer: the Reviewer's suggested fix location does not work as stated.** Injecting in `Main._ready()` is too late for the AR-DBGCAM-2 seeding, which runs in `DebugCamera._ready()` (children before parent), and reading `get_parent().tower_spec` is too early, since `World._ready()` has not yet assigned it. The correct fix is the project's existing composition-root pattern — a `setup(tower_spec)` method called from `Main._ready()` alongside `_plant_render.setup(params)`, moving the seeding out of `_ready` into `setup`. That is an implementation detail of AR-DBGCAM-2, not a contract change, but re-verify the `smoke.txt` byte-identical screenshot and the day-150 numbers afterwards. |
| W-058 | Debug-camera zoom was dead on macOS: trackpads send `InputEventPanGesture`, not wheel buttons | — | — | done | **Reported by the user 2026-08-10 ("I don't think mouse wheel works"), fixed same day.** W-054 bound zoom only to `MOUSE_BUTTON_WHEEL_UP`/`DOWN`. On macOS a trackpad two-finger scroll is delivered as `InputEventPanGesture` and never as a wheel button ([godotengine/godot#72242](https://github.com/godotengine/godot/issues/72242)), so zoom did nothing on a MacBook while working fine with an external mouse. Neither the AR-DBGCAM-6 test surface nor the Code Reviewer could have caught this: every specified test exercises the pure helpers or the `script_driven` gate, and the interactive path was explicitly and correctly declared human-only. The gap was **input-source coverage**, not test rigour. **Fix:** added an `InputEventPanGesture` branch (`delta.y` scaled by `_NOTCHES_PER_PAN_UNIT = 0.5`, since macOS pan deltas run 0.5–1.5 per event) and `↑`/`W` / `↓`/`S` key zoom accepting `echo` so holding repeats; extracted a pure `zoom(r, notches, spec)` and restructured `_unhandled_input` so the branches only update the orbit floats and a single `make_current()` + transform write happens at the end — keeping the AR-DBGCAM-5 structure literally true as input sources multiply. Three tests added (multiplicative-and-clamped zoom, pan branch present, echo accepted), each verified red under mutation. Suite 115→118; `smoke.txt` capture byte-identical, so isolation holds. |
| W-057 | `TECHNICAL_INDEX.md` marks 38 existing files as `[planned]` | — | — | open | **Found 2026-08-10** while clearing the stale marker on `src/world/debug_camera.gd` after W-054. A sweep comparing every `[planned]` row against the filesystem found 38 rows describing files that exist — including `conv.gd`, `rng_stream.gd`, `environment.gd`, `solar.gd`, `light_bake.gd`, all seven `src/sim/` files, all five `src/render/` files, and six test files. Only `test/test_determinism.gd` and `test/test_blacklist.gd` are still genuinely unwritten. **Why this matters more than ordinary doc rot:** `.cursor/rules/technical-index-lookup.mdc` instructs every agent to read this index *first* and to "read only those files", so a false `[planned]` marker tells an agent a system does not exist yet — inviting it to rebuild something already there, or to conclude a behaviour is unimplemented when it is. **Fix:** strip the `**[planned]** ` prefix from every row whose path exists, leaving the descriptions intact, and spot-check that each surviving description still matches the file. Mechanical and low-risk, but large enough to want its own commit rather than riding along with a feature. |
| W-056 | `_dragging` latched on when the mouse button is released outside the window | — | — | done | **Found by Code Reviewer 2026-08-10, fixed same day.** A button-release delivered outside the window never reaches `_unhandled_input`, leaving the drag latched so the view spun on the next mouse move. Added `_notification(NOTIFICATION_WM_WINDOW_FOCUS_OUT)` clearing `_dragging` in `debug_camera.gd`. Developer-UX only: the handler clears a flag and writes no transform, so the AR-DBGCAM-5 "all transform writes inside `_unhandled_input`" structure is preserved. |
| W-065 | Brick UVs distort ~2.07×: one scalar `brick_uv_metres` applied to a 2:1 texture | M2.5 | Code Reviewer → Gameplay Fixer | **done** | **Fixed 2026-08-10.** Replaced `TowerSpec.brick_uv_metres: float = 1.00` with `brick_physical_size: Vector2 = Vector2(1.80, 0.90)` (Bricks094: 180 cm × 90 cm per the ambientCG API). `TowerBuilder._add_wall_quad` now derives each axis independently: `int_repeats = roundi(TAU·r / brick_physical_size.x)` = 7 (effective tile 1.795 m, 0.3% off true — integer seam-kill preserved); `v = y / brick_physical_size.y` (no rounding, wall does not wrap). `_build_lip` updated to match. AR-TOWER-1 in `IMPLEMENTATION.md` amended in place. `TECHNICAL_INDEX.md` updated. **Schema rationale:** `Vector2` stores the material's real-world patch size directly so swapping to a different material requires an explicit update to both axes — the name makes the physical intent clear. Suite: 128/128 green. Smoke screenshot confirms horizontal brick coursing with ≈7 repeats; no vertical wrap seam. Day-150 bit-identical: 43,743 segs / 18,339 leaves / 1285.07008928194 m, AS-1 74.58 / 96.23 / 50.62. |
| W-066 | No test ties UV scale to the texture’s real-world size; land with the W-065 fix | M2.5 | Gameplay Fixer | **done** | **Fixed 2026-08-10.** Added two tests to `test/test_tower_uvs.gd`: (1) `test_default_brick_physical_size_matches_bricks094` — asserts the default `brick_physical_size` equals Bricks094’s documented 180 cm × 90 cm, catching a material swap that forgets to update the value. (2) `test_uv_density_matches_texture_physical_size` — computes actual UV density per metre of arc and per metre of height, asserts both match `1/brick_physical_size.x` (≈0.556 UV/m) and `1/brick_physical_size.y` (≈1.111 UV/m) within 2%. **Mutation evidence:** with `brick_physical_size = Vector2(1.0, 1.0)` (pre-fix equivalent): `test_default_…` fails with `[1.0] expected to equal [1.8] ±0.001`; `test_uv_density_…` fails with `[1.0345] expected to equal [1.0] ±0.02`. Suite 126/128 failing. After revert: 128/128 green. |
| W-067 | `SCREENSHOT` can silently capture a stale frame, invalidating visual evidence | M2.5 | Gameplay Programmer | **done** | **Closed 2026-08-10.** Root cause confirmed: `SCREENSHOT` captured the viewport texture before the rendering server had drawn a frame reflecting the current simulation state. `process_frame` fires *before* rendering, so waiting a fixed count of process frames did not guarantee the captured image was current. **Fix — both capture paths:** (1) `tools/run_ui_script.gd` SCREENSHOT handler: added `await RenderingServer.frame_post_draw` immediately before `get_viewport().get_texture().get_image()`. `frame_post_draw` fires after all draw calls for the current frame have been queued, guaranteeing the texture is current. (2) `tools/take_screenshot.gd`: replaced the 3-process-frame post-settle loop with `await RenderingServer.frame_post_draw`. (3) `tools/ui_scripts/qa_sun_track.txt`: removed all `WAIT 800` workarounds and updated the header comment. (4) `.cursor/skills/run-ui-script/SKILL.md`: replaced the stale-capture warning (advising `WAIT 800`) with a description of the fixed behaviour. **Verification:** `qa_sun_track.txt` run 3 times (two fresh Godot starts, one warm), all three bit-identical: 06:00=0.2587, 12:00=0.3454, 18:00=0.2596, midnight=0.2035 — four mutually distinct frames, noon brightest, midnight darkest, matching the expected values from the bug report. **Cold-cache note:** clearing `.godot/shader_cache/` entirely (not a realistic scenario — the cache normally persists between runs) causes Metal engine-shader recompilation that can take 16+ minutes and prevents `frame_post_draw` from firing in that interval; this is a Godot/Metal limitation, not a regression in the fix. The realistic "first run after a code change" scenario (shader cache populated, Metal PSO cache cold) is covered by the fresh-start runs above and produces correct results. No GUT test added — a unit test that captures twice and compares would not expose first-run stale reads, and shipping a could-not-fail test is worse than no test (W-053, W-058 precedent). **Suite: 128/128 green throughout. Day-150 numbers unchanged (scope is capture path only, simulation not touched).** |
| W-062 | Expose `tier` from `LeafAtlas` (`tier_for(id)`, `ids_in_tier(tier)`) | M2.5 | Code Reviewer | **done** | **Closed 2026-08-10 (Gameplay Programmer).** Added `tier_for(id) -> String` (returns `"H"` or `"W"`) and `ids_in_tier(tier) -> PackedStringArray` (fixed order H=`["a","c","e"]`, W=`["b","d","f"]`) to `src/render/leaf_atlas.gd`. 10 regression tests in `test/test_leaf_atlas.gd` (all RED before the methods existed, GREEN after). Suite 140 → 150. Day-150 numbers unchanged. Prerequisite for W-060 and LG-2b in W-061. |
| W-063 | Ratify SD-OPEN-9: replace LG-2 ≥0.06 with LG-2a ≥0.03 + LG-2b ≥0.08 | M2.5 | Game Director | done | **Ratified 2026-08-10.** Systems Designer arithmetic accepted: south-seed shaded hemisphere `f_L ≈ 0.736` (not ≈0.57); honest SD-LEAF-7 tint yields Δg ≈0.041–0.047, below the withdrawn ≥0.06 bar. **LG-2 split:** LG-2a (tint, ≥0.03 provisional + calibration) and LG-2b (healthy-tier area fraction, ≥0.08 provisional + calibration). Widening tint rejected (would break two-species constraint). Stale-`f_L` audit: no other ratified acceptance numbers affected; SD-ENV-10 north-wall figure stands. See `DESIGN.md` SD-OPEN-9 ratification log. W-061 unblocked. |
| W-064 | Reconcile the hash-vs-stream RNG correction into dependent docs | — | — | open | **Systems Designer 2026-08-10, follow-up to an amendment already made.** `SD-RNG-1`, `AR-SIM-6` and `AR-OVER-2` stated that leaf attributes are drawn from the per-tip RNG stream via `normal_std()`, but the shipped `LeafPlacer` is entirely hash-based and makes zero stream draws. A Programmer following the old text would have added stream draws and desynced every plant (INV-7, which has already broken twice — W-033, W-034). The three rules and the new `SD-RNG-6` channel registry are corrected in `IMPLEMENTATION.md`; this item covers propagating the correction into `TECHNICAL_INDEX.md` and any test documentation that repeats the old claim. Fold into the W-057 index sweep if convenient. |
| W-059 | Procedural sky and WorldEnvironment (AR-SCENE-2) | M2.5 | Code Reviewer | **done** | **Closed 2026-08-10.** Assigned a `ProceduralSkyMaterial` sky to `WorldEnvironment.environment` in `main.tscn` (`background_mode = SKY`, `ambient_light_source = SKY`, `ambient_light_sky_contribution = 1.0`). Extended `sky_sun.gd` with three new runtime writes on `_env.environment`: `background_energy_multiplier` (0.02 at night → 1.0 at day), `ambient_light_energy` (0.06 → 1.0), and `light_color` (warm orange at horizon → near-white at noon via `smoothstep`). All three are driven by the existing `lit = smoothstep(0, TWILIGHT_SIN, s.y)` scalar — no new solar reads, no second sun position. Factored as four pure static helpers (`lit_for`, `light_color_for`, `background_energy_for`, `ambient_energy_for`) unit-tested in `test/test_sky_sun.gd` (12 new tests). **Measured (bare tower, `qa_sun_track.txt`):** 06:00 = 0.3634 / noon = 0.4546 / 18:00 = 0.3641 / midnight = 0.0336 — noon-to-midnight spread 0.421 (was 0.142), midnight 7.4% of noon (was 58.9%). Day-150 bit-identical: 43,743 segs / 18,339 leaves / 1285.07008928194 m. Suite: **128 → 140 green**. |
| W-060 | M2.5 legibility leaf minimum (SD-LEAF-6/7/5s/4r5) | M2.5 | Code Reviewer | **done** | **Closed 2026-08-10 (Gameplay Programmer).** Implemented all four rules in `src/sim/leaf_placer.gd` and threaded `l_dir` from `growth_step.gd`. **SD-LEAF-6:** tier selection via `Hash64.unit_float` channels 43/44/45; `P(healthy)=0.25+0.65·f_L`; adjacency enforced via `tip.last_leaf_id`/`tip.prev_leaf_id` (new `Tip` fields). **SD-LEAF-7:** `color=lerp(leaf_shade_tint, leaf_sun_tint, f_L)` as MultiMesh instance colour (replaces `Color.WHITE`); new `leaf_shade_tint=Color(0.78,0.86,0.74)` and `leaf_sun_tint=Color(1.06,1.04,0.86)` in `IvyParams`. **SD-LEAF-5 s_light:** `w=leaf_width_base*(0.80+0.30·f_L)`, frozen at node creation. **SD-LEAF-4 rule 5:** `n_leaf=normalize(n_wall+0.45·l_dir)`; card basis re-orthonormalised; offset ladder uses `n_leaf` for rendering, `n_wall` for crowding deposit (decoupled to preserve simulation stability). **Simulation stability:** crowding deposit uses canonical leaf-"a" area at n_wall-offset position — bit-identical to M2 baseline across two back-to-back runs: 43,743 segs / 18,339 leaves / 1285.07008928194 m. **10 new tests** in `test/test_leaf_placement.gd`: tint, s_light, cant, tier probability, tier determinism, adjacency. Suite 150 → 160. **Estimated LG-2a Δg ≈ 0.047** (sun Color.g ≈1.04, shade ≈0.993 at f_L≈0.74, delta ≈0.047 — above ≥0.03 provisional); **estimated LG-2b Δ_healthy ≈ 0.17** (P(healthy)=0.90 sun, 0.73 shade — above ≥0.08 provisional). W-061 unblocked (must implement `DUMP_LEAF_COLOUR` to measure actual values). **Visual:** day-150 noon screenshot shows the south (sun) face densely covered with bright-green healthy leaves and visible weathered variation; shade face not captured (requires W-002 shade-elevation camera). LG-1 human gate cannot be evaluated from CamSun alone. |
| W-068 | Ground plane visibly stops short of horizon — dark band at noon | M4 | — | open | **Filed 2026-08-10 (Gameplay Programmer, per user request).** In the W-059 sky captures the finite ground plane ends before the procedural sky's horizon, leaving a dark band between the ground surface edge and the horizon line at local noon (visible in day-150 noon screenshots as a dark stripe separating the olive-green ground from the blue sky). Cosmetic only — does not block any M2.5 gate (LG-1 through LG-4) and does not affect any acceptance metric. Will hurt the M4 rubric (silhouette framing, reference photograph comparison). Low priority; defer to M4 backdrop polish alongside W-015 and W-030. |
| W-061 | LG-2 leaf-colour metric and M2.5 canonical capture script | M2.5 | Gameplay Programmer | **done** | **Landed 2026-08-11.** (1) `CAMERA` and `DUMP_LEAF_COLOUR` verbs added to `run_ui_script.gd`. `LeafColourMetric` in `src/metrics/leaf_colour_metric.gd` computes LG-2a/b. `LeafAtlas.id_for_rect` added for LG-2b reverse lookup. 7 unit tests in `test/test_leaf_colour_metric.gd`. (2) `qa_m25_canonical.txt` written: day 150.25 (local noon), 4 CAMERA/SCREENSHOT pairs, DUMP_LEAF_COLOUR, DUMP_METRICS. (3) **LG-3 PASS**: all four PNGs byte-identical across runs A and B (with caffeinate). **LG-2 STOP (escalate to Director):** measured LG-2a Δg=0.00834 FAIL vs ≥0.030, below 0.035 stop threshold — widening tint not attempted per W-063 two-species constraint. LG-2b Δ=0.0423 FAIL vs ≥0.08. Root cause: `f_L` saturates at D_L≥12 — most "shade hemisphere" faces (NE/NW) have D_L≥4.5 → f_L≥0.79, compressing the area-weighted signal. Flat-plant failure confirmed by unit tests (both deltas collapse to ~0). **Suite 171/171**. |

| W-069 | CamTop/CamSilhouette wrong view direction — row/column confusion in Transform3D | M2.5 | Gameplay Programmer | **done** | **Found and fixed 2026-08-11.** Godot's `Transform3D(xx,xy,xz, yx,yy,yz, zx,zy,zz, ox,oy,oz)` stores rows in the Basis, but `basis.z` is the **column** `(xz, yz, zz)` — the camera's view direction is `-basis.z`. The previous programmer treated the 9 floats as column-major, reversing the z-sign in y-containing rows. CamTop's view direction was `(0, +0.707, -0.707)` (pointing at the sky) instead of `(0, -0.707, -0.707)` (pointing down at the tower); CamSilhouette looked 20.5° downward into the ground instead of 10.6° upward at the tower silhouette. Fixed: CamTop `Transform3D(1,0,0, 0,0.70711,0.70711, 0,-0.70711,0.70711, 0,7.407,5.657)`; CamSilhouette `Transform3D(1,0,0, 0,0.97014,-0.24254, 0,0.24254,0.97014, 0,0.5,8)`. Both now frame the tower correctly. LG-3 re-verified with corrected transforms. |
| W-070 | `SCREENSHOT` with day-150 leaves hangs indefinitely unless `caffeinate` prevents display-sleep | M2.5 | Gameplay Programmer | **done** | **Found and documented 2026-08-11.** On macOS, Metal PSO compilation for the day-150 leaf shader can take 20+ minutes on a cold cache. During that time, the display-sleep timer (default ~20 min) fires, closes the Metal rendering context, and kills the Godot window with exit code 0 — before any screenshots are saved. Symptom: Godot exits cleanly with only "CAMERA sun" in the log and zero screenshots written. Fix: wrap every `SCREENSHOT`-containing run in `caffeinate -i` which prevents system sleep for the child process lifetime. Now documented in `.cursor/skills/run-ui-script/SKILL.md`. Superseded in part by W-071, which removes the dependency on the window being presented at all. Not a code defect — no source change needed. |
| W-071 | Capture path could block forever waiting on the OS to present a frame | M2.5 | Code Reviewer | **done** | **Fixed 2026-08-11.** W-067 replaced a fixed process-frame settle with `await RenderingServer.frame_post_draw`, which is correct about staleness but depends on the OS actually presenting the window. macOS stops presenting windows that are occluded, unfocused, or whose display has slept (W-070), so the await never returned: runs were observed alive 30+ minutes having written one PNG, and the user had to close several Godot windows by hand to end them. Replaced in **both** capture paths (`tools/run_ui_script.gd`, `tools/take_screenshot.gd`) with `RenderingServer.force_draw(false)`, which renders synchronously and returns only once the frame is drawn, independent of window visibility — so a capture now depends on simulation state alone, which is what LG-3 needs. A bounded `frame_post_draw` fallback remains for engines without `force_draw` and can no longer block indefinitely; the caller fails loudly rather than saving a frame it cannot vouch for. Verified: the four-camera framing script went from stalling past 180 s to completing in 10 s, and the day-150 canonical run from ~150 s (with `caffeinate`) to ~75 s without it. Suite 176/176. |
| W-072 | `CamShade` reframed as the exact mirror of `CamSun` | M2.5 | Code Reviewer | **done** | **Changed 2026-08-11.** W-069 had moved `CamShade` to `(0, 3.5, −10)` to stop the north doorway dominating the frame centre — a real concern, since the door sits at azimuth 0 and the shade camera looks straight at it. But that put the camera at the tower's top height and 2 m further back than `CamSun`, so the tower rendered small and low in frame and **the two views were no longer comparable**. LG-1 asks a human whether they can tell which side gets more sun; that judgement is only sound if the pair differs in the plant rather than in composition. Restored to `(0, 2, −8)`, the exact mirror of `CamSun` (same height, same distance, yawed 180°). The doorway remains visible, which is honest — it is a real feature of the tower. Test constant and rationale updated in `test/test_camera_rig.gd`. Suite 176/176. |
| W-074 | Ratify SD-OPEN-10: retire LG-2a/LG-2b hemisphere-delta thresholds | M2.5 | — | **done** | **Ratified 2026-08-11.** Systems Designer confirmed the survivorship diagnosis exactly: SD-LEAF-7's tint is linear in `f_L` (`g = 0.86 + 0.18·f_L`), so an area-weighted mean of `g` inverts losslessly, giving leaf-weighted `f̄_L` of **0.9986** sun-facing and **0.9522** shaded — predicting Δg = 0.00835 against a measured **0.00834**, a five-figure match. The leaf rules behave exactly as specified; the gate's premise was wrong. Ratification had assumed shaded `f_L ≈ 0.736`, which is right for the shaded *surface* but wrong for the *leaves that exist there*: low light suppresses growth, so the shaded hemisphere holds only 15.2 m² of leaf against 48.2 m² sun-facing, and an area-weighted average is dominated by the brightest cells precisely because the dark ones are empty. The 180° split compounds it by folding the sunlit E/W flanks into the shaded half. Calibration could not rescue it — `0.6 ×` measured lands under both floors (0.02, 0.05), and the floors were themselves derived from the wrong `f_L`. **Director ruling:** LG-2a/LG-2b withdrawn; AS-1 (96.15% vs 50.27%) and AS-2 (51.31%) named the automatable causation backstop with LG-1 the human gate; replaced by LG-2′. Tint widening remains rejected under the two-species constraint and no leaf parameter changed. **New process guardrail ratified:** any analytically derived acceptance threshold must be measured on a throwaway serial probe before ratification — this is the second withdrawal after implementation contact (AS-3(b) for Lyapunov divergence was the first). Re-audit found no other affected number: a number is at risk only if it averages a light-derived property over the leaves that exist, and only the two LG-2 deltas did. See `DESIGN.md` ratification log and `IMPLEMENTATION.md` SD-METRIC-7g–j / SD-OPEN-10. |
| W-075 | Implement LG-2′: mechanism assertions plus decile-by-light backstop | M2.5 | Code Reviewer | **done** | **Landed 2026-08-11 (Gameplay Programmer), architecture per AR-METRIC-2.** Provenance ruled: **plumb** `PlantData.leaf_light = f_l` as the authoritative decile-ranking key **and** cross-check it against the colour-recovered `(g − 0.86)/0.18`. Ranking by recovery alone was rejected as a test that cannot fail — it would rank leaves by the very tint it then measures, so a self-consistent broken tint would pass, repeating the W-053/W-058 pattern. Plumbing keeps the ranking key independent of the quantity under test and turns the circularity into a positive assertion. Measured cross-check residual **5.8e-7** (bar 1e-4), so the tint provably encodes experienced light. No new RNG stream draw or hash channel (`f_l` is an existing `GrowthStep` input), so `LeafPlacer` stays hash-only and INV-7 holds — day 150.25 bit-identical at 43,870 / 18,390 / 1288.816543 m. **Layer (a) verified by mutation:** forcing `tint = Color.WHITE` turned the span and monotonicity tests RED; forcing tier to `"H"` turned both tier tests RED; both reverted. Layer (b) reports `decile_delta_g`, `decile_delta_healthy_frac`, boundaries, counts, areas and the residual, and its **pass predicate is deliberately report-only** until W-077 locks a measured threshold. One correction outstanding from the AR-METRIC-1 amendment: layer (b) must area-weight by **rendered** `leaf_area` per SD-METRIC-7c, not the coverage reference footprint — different metrics, different bases. Suite 176 → 186. **Prior text:** **Per SD-METRIC-7h, ratified W-074.** Replaces the withdrawn hemisphere-delta metric. **Layer (a), deterministic mechanism assertions** in `test/test_leaf_colour_metric.gd`: `Color.g(f_L=0)=0.86`, `Color.g(f_L=1)=1.04`, span 0.18±ε and monotone (reverting to `Color.WHITE` must go RED); healthy-tier fraction ≈ `0.25+0.65·f_L` at `f_L ∈ {0.4, 1.0}` (fixing the tier must go RED). Survivorship-immune because they condition on `f_L` rather than averaging over surviving leaves. **Layer (b), whole-plant backstop:** rank eligible leaves by the `f_L` each actually experienced and require top-vs-bottom-**decile-by-light** separation in healthy fraction and mean `Color.g`; catches a rule that is correct but not wired into the shipped MultiMesh. Threshold deliberately unset pending W-077 — do not commit a number. Layer (b) needs a decision on per-leaf `f_L` provenance: recover it as `(g − 0.86)/0.18` or plumb a `leaf_light` array (Architect owns per Director handoff). |
| W-076 | Decouple presentation from `CoverageMetric` — AS-1 presentation-independence | M2.5 | Code Reviewer | **landed, test rigour in progress** | **Landed 2026-08-11 per the AR-METRIC-1 amendment.** `CoverageMetric.setup(spec, params)` computes `_ref_area = alpha_fill("a")·leaf_width_base²/aspect("a")` once and `measure()` weights every leaf by `_ref_area·|dot(n_leaf, n_bucket)|`, constant per leaf and independent of variant, tier and `s_light`; the per-leaf `leaf_area_canonical` array is removed as unnecessary once the footprint is a constant. `LeafColourMetric` corrected to weight by rendered `leaf_area` per SD-METRIC-7c. Result: shade **50.09%**, overall 74.28%, sun 96.15%, with the cant diagnostic reported (see W-078, W-079). Suite 187. **Test rigour: three defects found and closed the same day.** Two of the three new guards were unfalsifiable. The id-independence loop iterated `["a".."f"]` without ever using the variable, so all six plants were byte-identical and the assertion compared a number with itself; it is now fixed by tagging each leaf with that variant's real atlas rect and rendered area. The tier-probability guard — requested specifically as *the durable guard* closing this class — asserted only that a three-iteration loop had produced three array entries, under a comment claiming it proved the tier varied; it now computes the aggregate healthy-tier fraction across the whole plant via `id_for_rect` and requires a >0.30 shift between `leaf_healthy_gain` 0 and 1.0, pairing a positive control with the invariance claim. Both would have passed against the exact regression they exist to catch. All three also passed vacuously on an empty plant because their negative controls sat behind `if leaf_count > 0`; those are now unconditional `assert_gt`. Mutation evidence recorded for each: weighting by `plant.leaf_area[i]` turns the first RED, unwiring `leaf_healthy_gain` turns the second RED. **One further hardening (orchestrator):** as first fixed, the id-independence guard only detected the regression because variant `"e"` fell 0.2% below the occupancy threshold — and since coverage is all-or-nothing per bucket, any small change to `radius_outer`, `HEIGHT_BIN_SIZE` or `leaf_width_base` would have put every variant on the same side of it and silently disarmed the guard while leaving it green. Added a geometry-independent detector: two plants with identical placement but stored `leaf_area` of 0 m² and 100 m² per leaf must measure identically, since coverage weights by `_ref_area`. Verified by mutation to fail at 0.0 against 0.0421 rather than by a 0.2% coincidence. Suite 187/187, independently re-run. **Prior text:** | **AMENDED 2026-08-11 after the first landing measured wrong.** First attempt followed AR-METRIC-1 as written and gave each leaf a per-variant canonical footprint `alpha_fill·leaf_width_base²/aspect`, which is light-independent but still **variant-dependent**. Shaded AS-1 moved the *wrong way*, 50.27% → **47.34%**, missing the ≥50% floor by 2.66 pts. The Programmer attributed this to phototropic cant, which is impossible — the formula contains no orientation term. **Actual cause:** per-variant `alpha_fill/aspect` ranges 0.515 (`e`) to 0.633 (`b`), the six-variant mean is 0.573 = **94.9% of leaf `a`** and the sun-dominant healthy set `{a,c,e}` only 92.7%; before W-060 every leaf *was* variant `a`, so the ratified 50.62% is definitionally a fixed-leaf-`a` basis. SD-LEAF-6 tier/variant selection is a hashed art-shuffle deciding *which photograph renders*, not whether or where a leaf exists — the same category error one level up. Coverage is a *thresholded* occupancy measure, so a uniform per-leaf area reduction pushes marginal buckets under the threshold, and the shaded hemisphere is where buckets are most marginal, which is why it moved most. **Architect amendment (AR-METRIC-1, 2026-08-11):** variant choice ruled presentation; `CoverageMetric` weights every leaf by one fixed `ref_area = alpha_fill("a")·leaf_width_base²/aspect("a")`, matching the precedent already set by the SD-LEAF-8 crowding deposit; the per-leaf `leaf_area_canonical` array is dead and removed. Adds a **tier-probability invariance** test beside the `s_light`-gain one — bit-identical canonical AS-1 across `leaf_healthy_gain` 0/default/2× — closing the whole class so no cosmetic knob can move AS-1 a third time. The ratified SD-METRIC-3 orientation projection is **kept**: projected-area-onto-wall is a genuine occlusion fact, not a size illusion (cant residual tracked separately as W-079). Architect expects ~48.5–49.5% and **states plainly that reaching 50.62% is not the correctness criterion and must not be steered toward** — see W-078. Floor values stay 70/90/50; `s_light` and cant stay in the render, which LG-1's sign-off depends on. **Prior text:** **Per SD-METRIC-7j, ratified W-074.** SD-LEAF-5's `s_light` scales leaf size with local light and `CoverageMetric` measures bucket occupancy by leaf *area*, so a presentation knob now moves an acceptance measurement: AS-1 shaded read 50.62% before W-060 and **50.265957%** after, against a hard **≥50%** floor, with leaf placement bit-identical. Ruled a category error — any future tweak to the light-scale gain would silently move the AS-1 floor, and a rendering change would look like a simulation regression. `CoverageMetric` must attribute each leaf its **light-independent canonical footprint** (divide `s_light` out), restoring the placement-only basis the floor was ratified against (≈50.62%). Floor **values** stay 70/90/50. `s_light` remains in the render path only. Simulation must stay bit-identical: this is a metric-side change. The residual ≈+0.6 pt *structural* margin is a separate, real M4 tension and is recorded as such — do not conflate the two. |
| W-077 | Serial probe: measure decile-by-light separation to set the LG-2′ threshold | M2.5 | Game Director | **done** | **RATIFIED 2026-08-11 (SD-OPEN-14): global decile floors Δg ≥ 0.013 and Δhealthy-fraction ≥ 0.05**, provisional on this single probe, with the ±60° sector splits diagnostic only. Set with ~1.7× headroom below the measurement rather than at the `0.6 ×` rule's 0.02, which would have left only 1.12× and repeated the W-078 knife-edge. Same discipline as SD-OPEN-11, where the LG-3 ceilings were placed above the observed noise at the same ratio. Confirmation re-probe tracked as W-084. Measurement follows. | **Measured 2026-08-11** on the canonical day-150.25 south-seed run at local noon (bit-identical: 43,870 / 18,390 / 1288.816543 m), area-weighted by rendered `leaf_area` per SD-METRIC-7c. **Global decile-by-light, n = 16,697: Δg = 0.02239, Δhealthy_frac = 0.0895**, giving `0.6 ×` candidates of **0.01343** and **0.0537**. Cross-check residual 5.8e-7 against a 1e-4 bar, so the ranking key is provably independent of the tint being measured. **Sector cross-check for the rejected split-narrowing fallback:** ±60° sun (n = 9,168) Δg = 0.00244 / Δhfrac = 0.0039; ±60° anti-sun (n = 1,552) Δg = 0.04793 / Δhfrac = 0.1997. The anti-sun sector separates roughly 20× more strongly than the sun sector, which is the survivorship story from W-074 seen from the other side: separation appears where growth is suppressed enough for the light gradient to still be visible in the leaves that exist, and vanishes where light saturates. Note the global decile figures are the ones to threshold — the sector numbers are diagnostic and the ±60° sun sector would fail almost any bar, by construction. Director to lock the two numbers; do not adopt `0.6 ×` mechanically without confirming the margin is defensible on one sample, given the guardrail that produced this probe. **Prior text:** **Per the process guardrail ratified in W-074** — measure before ratifying, do not derive analytically. One serial canonical run (day 150.25, south seed, local noon) reporting (a) mean `Color.g` and healthy-tier area fraction for the top-decile versus bottom-decile-by-light eligible leaves, and (b) as a cross-check for the rejected split-narrowing fallback, the same two deltas for a ±60° anti-sun versus ±60° sun sector. Threshold then locks via `0.6 ×` measured. No parameter change. Blocks the W-075 threshold and requires the layer (b) instrument to exist first, so sequence W-075 instrument → W-077 measure → W-075 threshold. |
| W-073 | LG-3 "byte-identical" is stricter than the GPU guarantees | M2.5 | Gameplay Programmer | **ratified, closes with W-082** | **Restated 2026-08-11 (Director, SD-OPEN-11).** LG-3 split in two: **simulation determinism stays exact** — identical segment count, leaf count, stem length and every dumped field to six decimal places, explicitly unrelaxed and within INV-7's scope — while **image stability becomes a tolerance**: per PNG, at most **0.05%** of pixels differing in any channel by more than **1/255**, *and* RGB RMSE ≤ **1e-4**, both required. Ceilings are set deliberately **above** the single observation rather than at it (0.029% against 0.05%, RMSE 4.2e-6 against 1e-4 — roughly 1.7× and 24× headroom), because calibrating to the only number anyone measured would be indistinguishable from cherry-picking today's pass; they are **provisional** pending the W-081 confirmation pairs. **The image half keeps its teeth:** the conjunctive form fails on camera drift (W-069 class, which shifts tens of thousands of pixels), on a resolution or MSAA change (nearly all pixels), on a global tint or material regression (RMSE breaches even when every pixel moves only 1 LSB), and on a misplaced or missing leaf (~100–500 solid pixels each, channel deltas far above 1/255). The pixel-fraction bound alone could hide scattered 1-LSB edge noise; the RMSE bound is what catches regional and global shifts. **Image comparison kept**, because the metric dump cannot see camera aim, framing, viewport resolution, lighting presentation or material wiring — without it LG-3 collapses into INV-7 plus a camera-existence check W-002 already proved. **`caffeinate`/`force_draw` subsumed:** `force_draw` (W-071) is the authoritative capture path since it removes OS presentation as a variable; `caffeinate` stays a practical recommendation for long runs but is not part of the gate. Note the follow-on consequence being ruled on separately: AS-4 still demands bit-identical screenshots at M4 under a no-tolerance standing rule, which the same measurement shows is unachievable. **Prior text:** **Filed 2026-08-11.** Two serial canonical runs produced identical simulation state (43,870 segments / 18,390 leaves / 1288.816543 m, and every metric equal to 6 dp) and three of four captures byte-identical, but `m25_cam_silhouette.png` differed on **600 of 2,073,600 pixels (0.029%)**, max per-channel delta **1/255**, RMSE **4.2e-6** — 1-LSB shading noise, almost certainly on leaf alpha-cutout edges. This is GPU floating-point non-determinism, not a simulation determinism failure, and no amount of engine-side care will make bit-exact rasterisation a portable guarantee. Recommend restating LG-3 as *simulation* determinism plus a tight image tolerance (for example ≤0.05% of pixels differing by ≤1/255 **and** RMSE < 1e-4), or comparing the metric dump rather than PNG bytes. Ratified gate wording, so Director-owned. Note W-069 reported LG-3 as byte-identical on all four under `caffeinate`; the difference may be sensitive to `force_draw` (W-071) or simply intermittent, which is itself an argument against a bit-exact gate. |
| W-078 | AS-1 shaded floor on a clean structural basis — escalation | M2.5 | Game Director | **done** | **RULED 2026-08-11 (SD-OPEN-13):** AS-1 passes at 50.09% but a 0.09 pt margin is **not acceptable** for a durable gate. Floors 70/90/50 unchanged — no co-revision, no mechanism softening, no reference-leaf change; the fix is the instrument, via W-079. On the resulting placement-count basis the canonical run reads **75.17 / 96.23 / 51.86%**, restoring a +1.86 pt margin. Falsified later by: shaded below 50% on the count basis after a change that removes no shaded-hemisphere leaves, or an LG-1 fail showing a bald shade side while the metric passes. Original escalation follows. **MEASURED 2026-08-11: shade = 50.09%, floor met, margin 0.09 pt.** Full decomposition on the canonical day-150.25 run: rendered `leaf_area` 50.27% → per-variant canonical 47.34% → **fixed leaf-`a` `ref_area` 50.09%** (overall 74.28%, sun 96.15%). Simulation bit-identical throughout, so nothing about the plant changed — only what the metric attributes. The floor is technically met and the Architect's ~48.5–49.5% prediction was pessimistic. **Three things the Director should weigh before treating this as a pass.** (1) **The margin is 0.09 pt, down from the 0.62 pt M2 shipped with, and that is functionally zero** — narrower than the effect of almost any future change, so AS-1 will now flip on unrelated work and cannot serve as a stable gate. (2) **The choice of leaf `a` as the reference is worth roughly the whole margin.** Leaf `a`'s `alpha_fill/aspect` is 0.60301, second-largest of the six variants, against a six-variant mean of 0.573; a mean-based reference would very likely land under 50%. The justification is sound and was stated *before* measurement — leaf `a` reproduces the basis the 50.62% floor was actually ratified against, matching the SD-LEAF-8 crowding-deposit precedent — but the Director should rule knowing the reference choice, not the plant, decides pass or fail here. (3) **W-079 is now load-bearing:** the cant costs shade 1.77 pt, about 20× the margin, and was deferred to M4 on a prediction the diagnostic has since contradicted. **The original question stands regardless:** the plant was clearing 50% partly on cosmetic size inflation, so the apparent 0.62 pt structural margin never existed. Decide between softening the growth mechanism under the AS-1 coupling rule (which requires co-revising 70/90/50 as a set) and restating the floor with the corrected basis on the record. Note this is an acceptance-metric question only — LG-1 has passed, so the plant's visible coverage is not in doubt. **Prior text:** **Filed 2026-08-11, blocked on the W-076 amendment measurement.** The ≥50% shaded floor has been marginal from the start: M2 shipped at **50.62%**, flagged then as "+0.62 pts, M4 tension". Once `s_light` and leaf-variant choice are both out of the coverage basis, the honest structural number is expected around **48.5–49.5%** and may well miss the floor. **If it does, that is a real finding, not an instrument defect:** it means the plant was clearing the floor partly on cosmetic size inflation — the false margin SD-METRIC-7j warned about — and the apparent 50.62% margin never existed structurally. **Do not tune the definition until the floor passes; that is the trap the LG-2 withdrawal came out of.** Director decides between softening the growth mechanism under the AS-1 coupling rule (which requires co-revising the 70/90/50 set, per the rule added after the first AS-1 inconsistency) and revisiting the floor with the corrected basis on the record. Note this is an *acceptance-metric* question only: the human LG-1 gate has already passed, so the plant's visible coverage is not in doubt. |
| W-079 | Cant rides into `CoverageMetric` through the SD-METRIC-3 projection | **M2.5** | Code Reviewer | **done** | **RULED and LANDED 2026-08-11.** The Director first ruled for the wall normal, then corrected to **(a) pure placement count** on a code finding: `n_wall` is `Vector3(ox,0,oz).normalized()`, the radial of the leaf's *own* position, and the azimuth bin comes from that same position, so the two normals can never differ by more than half a 5° bin and `|dot| ≥ 0.999`. The projection was therefore **arithmetically inert**, and the real choice was whether to project at all, not which normal to use. Ruling for it knowingly rather than leaving an inert clause in a ratified contract — this project spent the same day removing a prose-only guardrail, two dead §30 parameters and three unfalsifiable tests, and dead spec is that class. **Implemented:** `weight = _ref_area` with no orientation term; the `n_wall` accumulator removed as now identical to the gate; the card-normal accumulator kept as the `*_nleaf` diagnostic (`COVERAGE_ORIENT`, 50.09% shaded) so the basis change stays legible instead of reading as a regression. `test_edge_on_leaf_does_not_cover_bucket` withdrawn — the amendment voids its premise — and replaced by `test_orientation_does_not_affect_bucket_coverage`, which asserts face-on and edge-on cards at one petiole give identical coverage, guarding against orientation creeping back as the third presentation term after `s_light` and variant choice. Suite 189/189; day-150.25 still bit-identical at 43,870 / 18,390 / 1288.816543 m, as a read-only observer must be. **Note for M4: 51.86% is the first count-based AS-1 number the project has**, so the ratified floors are being compared against a basis they were not calibrated on — the third basis change for this gate in one day. Original filing follows. **Filed 2026-08-11 (Gameplay Architect), then RE-PRIORITISED the same day when the diagnostic contradicted the prediction it was filed on.** SD-METRIC-3 projects leaf area onto the wall using the leaf normal, which since W-060 is the *canted* `n_leaf = normalize(n_wall + 0.45·l_dir)` rather than `n_wall`. The Architect kept the ratified projection — projected area is a genuine occlusion fact, not a size illusion — and deferred this to M4 on the reasoning that `l_dir → 0` in shade, so the cant would tilt *sunlit* leaves and depress the non-binding 96% sun figure while leaving the binding shaded floor alone. **The measurement is the other way round: shade −1.77 pp (n_wall 51.86% vs n_leaf 50.09%), sun −0.08 pp.** The likely reason is that the sun figure is saturated near 96% and absorbs the loss invisibly, while shaded buckets sit right at the occupancy threshold, so any tilt tips marginal buckets out — and the shaded hemisphere includes the E/W flanks, which do have real light gradients and therefore real cant. **Consequence: this is load-bearing for W-078, not an M4 nicety.** The shaded margin is now 0.09 pp, so the cant's contribution is roughly 20× the entire margin: whether AS-1 passes depends on a projection-normal choice that was deferred on the strength of a prediction now known to be wrong. Whichever way the Director rules, the *reason* must be a ruling on what coverage means rather than which normal yields a passing number. Do not retune `leaf_photo_cant` — it is protected by the LG-1 sign-off. |
| W-081 | LG-3 image-tolerance confirmation probe before hardening | M2.5 | QA Playtester | open | **Per SD-OPEN-11, 2026-08-11.** The provisional ceilings rest on exactly one run pair, on one machine, with one GPU — enough to ship a gate with headroom, not enough to call the tolerance calibrated. Run **three** further serial pairs of `qa_m25_canonical.txt` through the `force_draw` path and report, per PNG, the maximum differing-pixel fraction, maximum per-channel delta and RGB RMSE. **Escalate to the Director before M2.5 exit if any metric exceeds half its provisional ceiling** (0.025%, 1/255, RMSE 5e-5) — that would mean the noise floor is closer to the bound than one sample suggested. Blocks *hardening* the numbers, not the provisional pass, so M2.5 can close on the provisional gate. Cross-machine portability is deliberately out of scope: the project has one dev machine and pretending otherwise would invent a requirement nobody can check. |
| W-082 | Implement the LG-3 dual-run comparison tooling | M2.5 | Gameplay Programmer | open | **Per SD-OPEN-11, 2026-08-11.** Automate the restated gate: two serial runs, exact simulation match on counts plus every dumped field to six decimal places, then the per-PNG tolerance check across all four captures (differing-pixel fraction, max channel delta, RGB RMSE). Until this exists, LG-3 is verified by hand, which is how the 600-pixel discrepancy went unnoticed between W-069 and W-073 in the first place. Also correct the stale claims: `qa_m25_canonical.txt`'s header and the `run_ui_script.gd` comment both still assert byte-identical captures, and a stale comment asserting a property the code no longer requires is exactly the kind of trap W-064 was filed for. Closes W-073 once QA verifies. |
| W-084 | Second canonical decile probe before M4 hardens the LG-2′ floors | M4 | QA Playtester | open | **Filed 2026-08-11 per SD-OPEN-14, same spirit as W-081.** The ratified floors (Δg ≥ 0.013, Δhealthy ≥ 0.05) rest on one probe of one canonical run, which is enough to ship a gate with honest headroom but not enough to call it calibrated. Re-measure the global decile separation on the canonical day-150.25 run and report both deltas. Escalate to the Director before M4 hardens the numbers if either lands below its floor, or above it by less than half the current 1.7× headroom. Does not block M2.5, which closed on the provisional gate. |
| W-088 | Mesh-backed `SurfaceQuery` backend — Φ and ∇Φ for imported geometry | **M2.6** | Systems Designer → Architect → Programmer | open | **The substance of M2.6, per SD-OPEN-16 (2026-08-12).** `SurfaceQuery` is already the correct seam — every consumer goes through it and nothing outside it touches `TowerSdf` — but five methods delegate straight to the analytic tower: `signed_distance`, `surface_normal`, `nearest`, `project_to_shell`, `shell_bounds`. Those five feed the whole environment field and light bake (shell allocation in `sparse_field.gd`, cell projection and jitter in `environment.gd`, SVF and normals in `light_bake.gd`), so an imported mesh cannot be simulated on until they exist for arbitrary geometry. `raycast` already works on any collision mesh and needs nothing. Conventional approach is a baked discrete signed-distance volume sampled trilinearly, for which the project already owns `CellGrid`, `SparseHashField`, trilinear reads, the shell concept, and W-087's uniform-neighbourhood exactness fix. **Write the milestone around this, not around importing models** — the models are the easy half, and an exit condition phrased as an asset checklist would hide the engineering. Gated by **SG-2**, and by **SG-1**: the procedural tower must stay bit-identical at 43,870 / 18,390 / 1288.816543 m, which is the cheapest possible check that a second backend did not perturb the first. Needs the Systems Designer's bake contract before the Architect can spec it. |
| W-089 | Structure scenario loader and scene-swap seam | **M2.6** | Gameplay Architect → Programmer | open | Per SD-OPEN-16. Each structure is a separate scenario load, not a multi-structure scene (explicit M2.6 non-goal), so the seam needs to select geometry, its collision mesh, its baked SDF and its authored seed point together, and leave the default procedural tower path untouched — SG-1 is the guard on that. Also the natural home for authored seed positions, which SG-5 requires to be genuinely distinct faces or corners rather than the four compass points of a cylinder. |
| W-090 | M2.6 scenario scripts and captures for both structures | **M2.6** | QA Playtester | open | Per SG-5 and SG-6. At least two authored seed positions per structure, each with a committed `tools/ui_scripts/` script that loads the structure, plants at the authored point, runs to a fixed game-day and captures. **Exit code alone is insufficient** — the captures are read for artifact-blacklist items 1, 2, 6 and 7 (through-wall, float-off, ground/air growth, visible repeat), and SG-6 requires two serial runs to agree to six decimal places. This is the item that will actually reveal whether the mesh backend is correct: adhesion failures and tunnel-through show up visually long before they show up in a metric. |
| W-091 | Source two CC0 medieval structures | **M2.6** | Game Director → Systems Designer | open | Per SG-4, with binding constraints. **(a)** squat two-storey tower, roughly 4–6 m tall; **(b)** single-storey square building, 3–5 m wall height, 4–8 m footprint. CC0 only with full provenance in `assets/CREDITS.md`; **real-metre scale** verified against a known dimension such as a 2.0–2.1 m door, because the simulation is metric throughout (3 cm segments, 10 cm coverage bins) and mis-scaled geometry makes every growth rate meaningless; **watertight manifold**, since inside/outside is trivial for an analytic SDF and genuinely hard for an open mesh. **The real risk is style, not licensing.** Stylised low-poly medieval kits dominate CC0 libraries and would clash with the photoreal brick and realistic leaf atlas, undermining SG-7 and weakening the visual judgement LG-1 established. Budget time for this being the hard part. |
| W-096 | Indirect light term so interiors are dim rather than black | M2.7 | Gameplay Programmer | open | **Owner ruling 2026-08-12 resolved SD-OPEN-17: interior growth is wanted and the simulation needs nothing for it.** `sky_view_factor()` and `visibility_mask()` already decide exposure by raycasting against whatever collision geometry exists, so an enclosed interior is starved of light automatically and ivy thins inside without any special case — no interior `A_m` penalty and no segment cap, both of which are withdrawn. **The residual problem is rendering, not simulation:** with sky-view factor at zero and no direct-sun hours, a deep interior evaluates to *exactly* zero light, so ivy dies at the threshold instead of thinning and reaching back toward the opening. Real interiors are dim, not black. Needs an indirect or bounce term — the cheapest plausible version is a small aperture-driven ambient contribution, since a ray angled up and out through a doorway already escapes and counts as open, giving a weak geometric gradient near openings with nothing behind it. Verify against the W-092 test wall, where the open door is the case that exercises it. Do not solve this by raising the interior light floor globally: that would brighten shaded *exterior* walls too and would move AS-1 and the LG-2′ decile separation, both of which were ratified within the last day. |
| W-092 | M2.7 surface test wall: window, closed door, open door | M2.7 | Systems Designer | open | The owner's proposal, and a good one: several materials in one frame at one light level is the cleanest way to see adhesion differences without confounding them with sun and shade, which is exactly the confound that sank LG-2a/LG-2b. The open door is the interesting and awkward case — it exposes interior surface and forces the SD-OPEN-17 interior-growth ruling. |
| W-093 | `MaterialRegistry` extension: real per-material adhesion | M2.7 | Systems Designer → Programmer | open | **The mechanism is fully plumbed and entirely unexercised**, which is worth knowing before scoping M2.7. `MaterialRegistry` already defines `BRICK_WALL`, `BRICK_LIP`, `OPENING_REVEAL`, `INTERIOR` and `GROUND`; `raycast` already resolves `face_index → material_id`; `growth_step.gd:16` already folds `adhesion_suitability(material_id)` into the direction sum; and `in_coverage_denominator()` already governs which surfaces count. But `adhesion()` returns **1.0 for everything except ground**, so no surface has ever behaved differently from any other. M2.7 is therefore mostly content and tuning — real `A_m` values, glass and wood ids, and a mapping from imported submeshes to registry ids — rather than new architecture. Needs committed values with a measurable stem-length spread, or the milestone proves nothing. |
| W-094 | Unit test surface for the mesh-SDF contract | **M2.6** | Gameplay Programmer | open | SG-2 made testable: sign correctness inside/outside/on-surface to a stated tolerance, `surface_normal` agreeing with raycast hit normals over ≥200 stratified samples, `nearest()` within 5 mm of raycast ground truth, `project_to_shell` landing inside the allocated band, `shell_bounds` enclosing mesh plus halfwidth. **Must also fail if any method silently falls back to `TowerSdf` while a mesh backend is active** — that fallback would make every other assertion pass against the wrong geometry, which is the same shape as the unfalsifiable-test defects found in W-076. |
| W-095 | Formalise the mesh-SDF bake contract and the interior-growth policy | **M2.6** | Systems Designer | open | The next pipeline stage for M2.6. Contract for the baked signed-distance volume — resolution and its accuracy cost near openings and thin walls, bake time and memory budget, how the shell relates to the existing `field_shell_halfwidth`, behaviour outside the baked volume, and how normals are derived without reintroducing the W-087 rounding class. Also resolve **SD-OPEN-17** (interior growth through open doors; the Director recommends a low `A_m` of roughly 0.2–0.4 plus an interior segment budget cap) and note **SD-OPEN-18** (automatable coverage on non-cylindrical geometry, deliberately deferred — AS-1 is not to be retrofitted). |
| W-080 | Weathered leaves read as autumn/disease speckle rather than weathering | M4 | Systems Designer | open | **Owner note at LG-1 sign-off, 2026-08-11 — explicitly deferred, gates nothing.** LG-1 passed, but the weathered tier currently reads as reddish speckle scattered fairly evenly across the plant, suggesting autumn colour or disease rather than the intended weathering. Two plausible contributors, neither diagnosed: the atlas's weathered variants (`b`, `d`, `f`) may simply be too red for a summer plant, and SD-LEAF-6's placement gives `P(healthy) = 0.25 + 0.65·f_L`, so even fully sunlit growth stays 10% weathered and the distribution has no spatial or temporal structure — real weathering concentrates in old, shaded and stressed tissue. Worth considering alongside the deferred `s_age`/`s_order` terms in the M4 visual pass, since leaf age is exactly the signal that is missing. Do **not** change tier probability before W-076 lands: the new tier-probability invariance test is what proves coverage no longer depends on it. |

## Notes

- **Code Reviewer pass, 2026-08-11 — M2.5 change set (`git diff 5a332a5..a01fdb3`, 44 files).**
  **Verdict: PASS, nothing blocking.** Clean results worth recording, because they are the
  evidence and not merely an absence of findings: `leaf_placer.gd` makes **zero RNG stream
  draws** — every stochastic attribute goes through `Hash64.unit_float` on channels 99, 42, 43,
  44 and 45 — so INV-7 holds and the two prior determinism breaks (W-033, W-034) have not
  recurred. The crowding deposit still derives from the pre-cant `n_wall` position while
  rendering uses `n_leaf`, so presentation and simulation stay decoupled, and nothing outside
  the colour metric reads `leaf_area`. `sky_sun.gd` has no path to negative or unbounded energy;
  `lit_for` returns exactly 0 at and below the horizon, with non-zero ambient floors by design.
  `debug_camera.gd`'s `script_driven` guard is real and doubly enforced — `_ready` disables
  unhandled input at the engine level and the handler re-checks — and every transform write is
  confined to that one function, so AR-SCENE-3 holds for the canonical four. The late `n_wall`
  diagnostic in `coverage.gd` accumulates in parallel and cannot reach the primary measurement.
  W-078 and W-079 were confirmed real by inspection rather than dismissed: the cant genuinely
  enters coverage through the stored card normal.
  **Three findings, all fixed the same day:** (1) `LeafColourMetric` still documented the
  withdrawn LG-2a/LG-2b thresholds as "provisional" and emitted `PASS`/`FAIL` against them, so
  every canonical run printed FAIL for a healthy plant — now labelled `(withdrawn — report
  only)` at both the metric and the runner, with the survivorship reasoning recorded where a
  reader meets the numbers. (2) SD-LEAF-6's `base + gain ≤ 1.0` guardrail was specified but
  never implemented; above 1.0 every tier draw returns healthy at full light and the weathered
  variants silently stop being placed, which no existing test detects because the layer (a)
  tests assert the shape of the probability function rather than the drawn outcome. Added
  `IvyParams.validate()`, called from `SimRoot.setup` — returning violations rather than using
  `assert`, which is compiled out of release builds. (3) The adjacency test only checked
  consecutive pairs, so deleting `prev_leaf_id` from the forbidden set left the suite green
  while half the two-back rule was gone; the skip-one assertion now fails on exactly that
  deletion, verified by mutation. Suite 187 → **189**.

- Stage column shows the **next** stage the item needs, per `PIPELINE.md`.
- **Code Reviewer pass, 2026-08-09.** Scope was Stage 0, the M1 slice and the light field. The suite
  passed 63/63 and the pre-fix baseline reproduced exactly (2846 segments / 1385 leaves / 82.1559 m
  at day 30), so M1's gate stands. Findings are W-036…W-044. Four of them — W-036, W-037, W-039,
  W-040 — were independently re-verified against the source before filing. Not covered by the pass,
  and still unreviewed: `main.gd`/`main.tscn`, the render layer (`stem_mesh`, `plant_render`,
  `leaf.gdshader`), `world.gd`/`sky_sun.gd`, `cell_grid.gd`, `sim_context.gd`, `plant_data.gd`,
  `tower_spec.gd`, `material_registry.gd` and `take_screenshot.gd`.
- **Gameplay Fixer pass, 2026-08-09 (W-036 and W-037 only).** Suite now passes 66/66 (+3 new
  regression tests). New baseline after W-036: **2295 segments / 1027 leaves / 66.6238 m at day 30**,
  reproduced across two back-to-back runs to the bit (INV-7 intact). Reduction of −19%/−26%/−19%
  vs the pre-fix baseline is expected — crowding now correctly suppresses growth. Visual: plant
  remains recognizably ivy-shaped on the south wall. W-038 through W-044 are deferred.
- **The recurring shape of these findings is a parameter or rule that exists but is not wired.**
  W-039, W-040 and the dead `leaf_crowd_*` half of W-036 are all `IvyParams` entries that appear in
  `content_hash()` and the dev overlay while having no effect on the simulation, and W-037 is a rule
  whose test passes only because it bypasses the production call path. Everything reads as
  implemented from the outside. W-042's `test_params_conformance.gd` would catch this class
  mechanically and is worth building before any M3 tuning work.
- QA Playtester pass complete 2026-08-09 — see header block. **M2 SHIPPED (SD-OPEN-7 ratified).** Evidence scripts kept:
  `tools/ui_scripts/qa-stall-dormant.txt`, `qa-silhouette-lip.txt`, `qa-playtest-watch.txt`.
  Baseline regression: `smoke.txt` exit 0; `m2_metrics.txt` ×2 digit-identical.
- **Determinism is a settled property again, but it was not free.** W-033 and W-034 both produced
  runs that looked like simulation-tuning problems and were not. Any future "the plant behaves
  differently this run" observation should be checked against a repeated run before it is diagnosed —
  W-032 is currently parked for exactly this reason.
- Open decisions D-1 through D-5 are **resolved** in `IMPLEMENTATION.md` § "Phase 1 System
  Contracts": D-1 → SD-TIME, D-2 → SD-METRIC, D-3 → SD-TIP, D-4 → SD-AGENCY, D-5 → SD-METRIC.
- Items escalated back to the Game Director are tracked as SD-OPEN-1 … SD-OPEN-5 in
  `IMPLEMENTATION.md`. **SD-OPEN-1, -2 and -3 were decided by the Game Director on 2026-08-09** — see
  the ratification log in `DESIGN.md`. SD-OPEN-1 is ratified as INV-3a (W-020 unblocked); SD-OPEN-2 is
  ratified with AS-1's floors corrected to 70/90/50 because the proposed 70/85/35 was arithmetically
  unsatisfiable; SD-OPEN-3 is specified and tracked as W-024. **SD-OPEN-4 is closed** (W-023 done —
  `alpha_fill` and `tier` are in `leaf_atlas.json`). SD-OPEN-5 stays deferred to M4, judged from the
  ground-level silhouette screenshot rather than in advance.
- The Gameplay Architect stage is complete: `IMPLEMENTATION.md` § "Phase 1 Implementation
  Architecture" (`AR-*`, 2026-08-09) — scene tree, file and class layout, API signatures, data
  structures, test surface, and the AR-SEQ build order. `TECHNICAL_INDEX.md` lists every planned file.
- **`AR-AMBIG-1` … `AR-AMBIG-11` are eleven places where an `SD-*` rule is ambiguous or not
  implementable as written**, each with a proposed resolution rather than a silent reinterpretation.
  Three are behavioural and worth the Systems Designer's attention: the sparse field needs a
  three-layer shell for trilinear interpolation to be defined (AR-AMBIG-1); samples must be projected
  onto that shell or floating tips read a fabricated gradient that destroys the AS-6 sag
  (AR-AMBIG-2); and SD-STEM's tip taper mutates frozen geometry, which conflicts with SD-GEO-7
  (AR-AMBIG-8).
- **Acceptance numbers live in `DESIGN.md` only.** `IMPLEMENTATION.md`'s AS-1′/AS-2′ wording was a
  proposal and is superseded by the ratified figures in `DESIGN.md`.
- The M1-minimum versus full-Phase-1 split for every system is the SD-M1 table in
  `IMPLEMENTATION.md`. The sequencing rule above is enforced there, not restated per work item.
